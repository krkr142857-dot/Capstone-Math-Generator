/**
 * geometry.js 
 * 고등학교 공통수학2 - 도형의 방정식
 * [FINAL STABLE VERSION] - OX 필수 속성 보강 및 함수 복구 완료
 */
(function () {
  const G = "고1";
  const U = "도형의 방정식";

  if (typeof HIGH_OX_BANK === "undefined") globalThis.HIGH_OX_BANK = {};
  if (typeof HIGH_BANK === "undefined") globalThis.HIGH_BANK = {};
  if (typeof HIGH_RAND_GENS === "undefined") globalThis.HIGH_RAND_GENS = {};
  if (typeof HIGH_GRADE_UNITS === "undefined") globalThis.HIGH_GRADE_UNITS = {};

  const _H_OX = (typeof HIGH_OX_BANK !== "undefined") ? HIGH_OX_BANK : globalThis.HIGH_OX_BANK;
  const _H_B = (typeof HIGH_BANK !== "undefined") ? HIGH_BANK : globalThis.HIGH_BANK;
  const _H_RG = (typeof HIGH_RAND_GENS !== "undefined") ? HIGH_RAND_GENS : globalThis.HIGH_RAND_GENS;
  const _H_GU = (typeof HIGH_GRADE_UNITS !== "undefined") ? HIGH_GRADE_UNITS : globalThis.HIGH_GRADE_UNITS;

  if (!_H_OX[G]) _H_OX[G] = {};
  if (!_H_B[G]) _H_B[G] = {};
  if (!_H_RG[G]) _H_RG[G] = {};
  if (!_H_B[G][U]) _H_B[G][U] = {};
  if (!_H_RG[G][U]) _H_RG[G][U] = {};

  const STD_01_01 = "[10공수2-01-01] 선분의 내분을 이해하고, 내분점의 좌표를 계산할 수 있다.";
  const STD_01_02 = "[10공수2-01-02] 두 직선의 평행 조건과 수직 조건을 탐구하고 이해한다.";
  const STD_01_03 = "[10공수2-01-03] 점과 직선 사이의 거리를 구하고, 관련된 문제를 해결할 수 있다.";
  const STD_01_04 = "[10공수2-01-04] 원의 방정식을 구하고, 그래프를 그릴 수 있다.";
  const STD_01_05 = "[10공수2-01-05] 좌표평면에서 원과 직선의 위치 관계를 판단하고, 이를 활용하여 문제를 해결할 수 있다.";
  const STD_01_06 = "[10공수2-01-06] 평행이동을 탐구하고, 실생활과 연결하여 문제를 해결할 수 있다.";
  const STD_01_07 = "[10공수2-01-07] 원점, $x$축, $y$축, 직선 $y=x$에 대한 대칭이동을 탐구하고, 실생활과 연결하여 문제를 해결할 수 있다.";

  const riSafe = (min, max) => Math.floor(Math.random() * (max - min + 1)) + min;
  const spNZ = (n) => (n === 0 ? "" : (n > 0 ? "+" + n : String(n)));
  const coef = (k, v) => (k === 1 ? v : (k === -1 ? "-" + v : (k === 0 ? "" : `${k}${v}`)));
  const sqShift = (v, c) => (c === 0 ? `${v}^2` : (c > 0 ? `(${v}-${c})^2` : `(${v}+${Math.abs(c)})^2`));

  const _gcd = (a, b) => b === 0 ? a : _gcd(b, a % b);
  const toFrac = (num, den) => {
    if (den === 0) return `${num}`;
    if (num % den === 0) return `${num / den}`;
    const sign = (num < 0) !== (den < 0) ? "-" : "";
    const an = Math.abs(num), ad = Math.abs(den), g = _gcd(an, ad);
    return `${sign}\\dfrac{${an / g}}{${ad / g}}`;
  };
  const formatNum = (val) => {
    if (Number.isInteger(val)) return `${val}`;
    for (let d = 2; d <= 100; d++) {
      const n = val * d;
      if (Math.abs(n - Math.round(n)) < 1e-9) return toFrac(Math.round(n), d);
    }
    return `${val}`;
  };

  function makeOx(q, ans, sol, hints, terms, std) {
    return { type: ["OX형"], q, choices: ["O", "X"], ci: ans === "O" ? 0 : 1, ans, sa: ans, sol, hints, terms, std };
  }
  function makeMc(q, choices, ci, ans, sa, sol, hints, terms, std) { 
    return { type: ["객관식"], q, choices, ci, ans, sa, sol, hints, terms, std }; 
  }
  function makeSa(q, ans, sa, sol, hints, terms, std) { 
    return { type: ["단답형"], q, choices: [], ci: -1, ans, sa, sol, hints, terms, std }; 
  }
  function makeEs(q, ans, sa, sol, hints, terms, std) { 
    return { type: ["서술형"], q, choices: [], ci: -1, ans, sa, sol, hints, terms, std }; 
  }
  function makeWrongChoices(ans, wrongs) {
    let pool = [ans, ...wrongs];
    pool = [...new Set(pool)].sort(() => Math.random() - 0.5);
    return { choices: pool, ci: pool.indexOf(ans) };
  }

  // --- [Lv1 기초 단계 OX 뱅크: 20문항] ---
  const lv1_OX = [
    makeOx("수직선 위의 두 점 $a, b$ 사이의 거리는 $|a-b|$이다.", "O", "두 점의 좌표의 차에 절댓값을 씌우면 거리가 된다.", ["수직선 위에서 두 점 사이의 거리를 구하는 정의를 확인한다.", "절댓값 기호를 사용하여 항상 양의 거리가 산출되는지 점검한다."], "수직선 위의 두 점 사이의 거리", STD_01_01),
    makeOx("좌표평면 위의 두 점 $(x_1, y_1), (x_2, y_2)$ 사이의 거리는 $\\sqrt{(x_2-x_1) + (y_2-y_1)}$이다.", "X", "각 좌표의 차를 제곱하여 더해야 하므로 $\\sqrt{(x_2-x_1)^2 + (y_2-y_1)^2}$가 옳다.", ["피타고라스 정리에 기반한 거리 공식에서 각 성분에 제곱이 포함되는지 확인한다.", "루트 안의 수치가 올바른 전개 방식인지 점검한다."], "두 점 사이의 거리 공식", STD_01_01),
    makeOx("기울기가 같은 두 직선은 항상 평행하다.", "X", "기울기가 같더라도 $y$절편까지 같으면 두 직선은 일치하므로 항상 평행한 것은 아니다.", ["평행의 정의는 '만나지 않는 것'임을 상기한다.", "기울기가 같을 때 일치하는 경우를 예외로 고려한다."], "두 직선의 위치 관계", STD_01_02),
    makeOx("두 직선이 평행하면 기울기가 서로 같다.", "O", "평행한 두 직선은 기울어진 정도가 같으므로 기울기가 동일하다.", ["두 직선이 평행하기 위한 기울기 조건을 다시 한번 확인한다.", "평행하면 기울기가 같음을 기초 성질로 기억한다."], "두 직선의 평행 조건", STD_01_02),
    makeOx("$x$축에 평행한 직선의 기울기는 $0$이다.", "O", "$x$축과 평행하면 $y$값의 변화량이 $0$이므로 기울기는 $0$이 된다.", ["기울기의 정의인 x의 변화량에 대한 y의 변화량의 비를 적용해본다.", "수평인 직선의 기울기 수치를 확인한다."], "직선의 기울기", STD_01_02),
    makeOx("점 $(a, b)$에서 $x$축까지의 거리는 $|b|$이다.", "O", "$x$축까지의 거리는 $y$좌표의 절댓값과 같다.", ["좌표평면에서 점의 좌표가 나타내는 기하학적 의미를 파악한다.", "축까지의 거리가 어떤 좌표값에 비례하는지 시각화한다."], "점과 축 사이의 거리", STD_01_03),
    makeOx("점 $(a, b)$에서 $y$축까지의 거리는 $|a|$이다.", "O", "$y$축까지의 거리는 $x$좌표의 절댓값과 같다.", ["y축으로부터 떨어진 거리가 x좌표와 관련이 있음을 이해한다.", "좌표평면상에서 점과 y축 사이의 간격을 확인한다."], "점과 축 사이의 거리", STD_01_03),
    makeOx("원 $x^2 + y^2 = r^2$의 중심은 원점 $(0,0)$이다.", "O", "기본형 원의 방정식의 중심은 원점이다.", ["원의 방정식의 기본형(x^2+y^2=r^2)에서 중심의 위치를 상기한다.", "중심의 좌표를 (0,0)으로 두었을 때의 식을 확인한다."], "원의 방정식의 중심", STD_01_04),
    makeOx("원의 방정식에서 반지름의 길이는 항상 양수여야 한다.", "O", "반지름은 길이를 나타내므로 항상 $0$보다 커야 원이 정의된다.", ["도형으로서의 원이 성립하기 위한 반지름의 조건(r>0)을 생각한다.", "길이가 0이거나 음수인 경우를 기하학적으로 검토한다."], "원의 정의", STD_01_04),
    makeOx("중심이 $(a, b)$이고 반지름이 $r$인 원의 방정식은 $(x-a)^2 + (y-b)^2 = r^2$이다.", "O", "표준형은 $(x-a)^2 + (y-b)^2 = r^2$이다.", ["원의 방정식 표준형에서 중심의 좌표가 식에 어떻게 반영되는지 대조한다.", "평행이동의 원리에 따른 부호 변화를 다시 점검한다."], "원의 방정식 표준형", STD_01_04),
    makeOx("원 $x^2 + y^2 = 4$의 반지름의 길이는 $2$이다.", "O", "우변은 반지름의 제곱($r^2$)이므로 반지름은 $2$이다.", ["원의 방정식 우변에 있는 상수의 제곱근을 구하여 반지름을 찾는다.", "반지름 r과 r^2의 차이를 명확히 구분한다."], "원의 반지름", STD_01_04),
    makeOx("점 $(x, y)$를 $x$축에 대하여 대칭이동하면 $y$좌표의 부호가 바뀐다.", "O", "$x$축 대칭이동의 규칙은 $(x, y) \\rightarrow (x, -y)$이다.", ["축 대칭이동 시 변하지 않는 좌표와 변하는 좌표를 구별한다.", "x축을 기준으로 접었을 때 점의 위치 변화를 상기한다."], "점의 대칭이동", STD_01_07),
    makeOx("점 $(x, y)$를 원점에 대하여 대칭이동하면 $(-x, -y)$가 된다.", "O", "원점 대칭은 $x$와 $y$의 부호를 모두 바꾼다.", ["원점 대칭이동에 따른 좌표 부호의 동시 변화 규칙을 확인한다.", "원점을 중심으로 180도 회전했을 때의 좌표를 생각한다."], "원점 대칭이동", STD_01_07),
    makeOx("점 $(x, y)$를 직선 $y=x$에 대하여 대칭이동하면 $(y, x)$가 된다.", "O", "직선 $y=x$ 대칭은 $x$와 $y$의 위치를 바꾸어 $(y, x)$가 된다.", ["직선 y=x 대칭이동 시 좌표의 값 자체가 교환되는 성질을 이용한다.", "부호 변화와 위치 변화를 혼동하지 않도록 주의한다."], "직선 y=x에 대한 대칭이동", STD_01_07),
    makeOx("평행이동에 의해 도형의 모양과 크기는 변하지 않는다.", "O", "평행이동은 위치만 바꿀 뿐 도형의 성질은 유지하는 합동 변환이다.", ["도형을 그대로 밀어서 이동시키는 평행이동의 기하학적 정의를 생각한다.", "모양과 크기가 유지되는 성질이 평행이동의 특징임을 기억한다."], "도형의 평행이동", STD_01_06),
    makeOx("두 직선이 수직이면 기울기의 곱은 $-1$이다.", "O", "두 직선이 수직일 조건은 기울기의 곱이 $-1$인 것이다.", ["직선의 수직 관계를 기울기로 나타낼 때의 공식을 암기한다.", "기울기의 곱이 1인 경우와 -1인 경우의 차이를 명확히 한다."], "두 직선의 수직 조건", STD_01_02),
    makeOx("두 점 $(a,b), (c,d)$의 중점의 좌표는 $(\\dfrac{a+c}{2}, \\dfrac{b+d}{2})$이다.", "O", "중점은 각 좌표의 산술 평균과 같다.", ["두 점의 한가운데 지점을 좌표의 합과 나눗셈으로 구하는 방식을 확인한다.", "선분의 중점 공식을 다시 한번 상기한다."], "선분의 중점", STD_01_01),
    makeOx("점 $(a,b)$를 $y$축 방향으로 $k$만큼 평행이동하면 $(a, b+k)$가 된다.", "O", "점의 이동은 해당 좌표에 이동량을 직접 더한다.", ["점의 좌표 평행이동 규칙을 y성분에 적용하여 계산한다.", "좌표 평면 위에서의 이동 방향과 부호의 관계를 확인한다."], "점의 평행이동", STD_01_06),

    // --- [동적 생성 문제 2개] ---
    (function() {
      const a = riSafe(-5, 5), b = riSafe(-5, 5);
      return makeOx(`점 $(${a}, ${b})$와 $(${a+3}, ${b+4})$ 사이의 거리는 $5$이다.`, "O", `$\\sqrt{3^2 + 4^2} = \\sqrt{25} = 5$이므로 참이다.`, ["피타고라스 정리에 기반한 두 점 사이의 거리 공식을 대입하여 연산한다.", "좌표 차이의 제곱 합이 반지름의 제곱과 같은지 점검한다."], "두 점 사이의 거리 계산", STD_01_01);
    })(),
    (function() {
      const x = riSafe(2, 6), y = riSafe(2, 6);
      return makeOx(`점 $(${x}, ${y})$를 $y$축에 대하여 대칭이동한 점의 좌표는 $(-${x}, ${y})$이다.`, "O", "$y$축 대칭이동은 $x$좌표의 부호만 반대로 바꾼다.", ["y축을 기준으로 대칭시켰을 때 변화가 생기는 좌표 성분을 파악한다.", "좌표의 부호 변화가 대칭 규칙에 부합하는지 확인한다."], "점의 y축 대칭이동", STD_01_07);
    })()
  ];

  // --- [Lv2 기본 단계 OX 뱅크: 정적 20문항 + 동적 5문항] ---
  const lv2_OX = [
    makeOx("선분 $AB$를 $1:2$로 내분하는 점은 선분 $AB$를 $2:1$로 외분하는 점보다 점 $A$에 더 가깝다.", "O", "내분점은 선분 내부에 있고 $A$에 가깝지만, $2:1$ 외분점은 $B$의 바깥쪽에 위치하므로 $A$에 더 가까운 것은 내분점이다.", ["내분점과 외분점의 기하학적 위치를 수직선에 그려서 비교한다."], "내분점과 외분점의 위치", STD_01_01),
    makeOx("세 점 $A, B, C$를 꼭짓점으로 하는 삼각형 $ABC$의 무게중심은 세 중선의 교점과 항상 일치한다.", "O", "삼각형의 무게중심의 기하학적 정의 자체가 세 중선의 교점이다.", ["삼각형의 무게중심의 기하학적 정의를 상기한다."], "삼각형의 무게중심", STD_01_01),
    makeOx("수직선 위가 아닌 좌표평면 위의 세 점 $A, B, C$에 대하여 $AB+BC=AC$가 성립하면, 세 점은 한 직선 위에 있다.", "O", "삼각부등식에서 등호가 성립하는 경우는 세 점이 일직선 상에 위치할 때뿐이다.", ["삼각형의 결정 조건과 두 점 사이의 거리의 최단 경로 성질을 이용한다."], "세 점의 위치 관계", STD_01_01),
    makeOx("두 직선 $ax+by+c=0$과 $a'x+b'y+c'=0$이 수직일 조건은 $ab' - a'b = 0$이다.", "X", "$ab'-a'b=0$은 평행 조건이며, 수직 조건은 $aa'+bb'=0$이다.", ["$ab'-a'b=0$은 평행 조건이며, 수직 조건은 $aa'+bb'=0$임을 구별한다."], "두 직선의 평행과 수직", STD_01_02),
    makeOx("점 $(x_1, y_1)$에서 직선 $ax+by+c=0$에 내린 수선의 발을 $H$라 할 때, 선분 $PH$의 길이는 점과 직선 사이의 거리와 같다.", "O", "점과 직선 사이의 거리의 정의가 곧 점에서 직선에 내린 수선(최단거리)의 길이이다.", ["수선의 발의 정의와 점과 직선 사이의 거리의 기하학적 의미를 파악한다."], "점과 직선 사이의 거리의 정의", STD_01_03),
    makeOx("두 직선이 평행할 때, 한 직선 위의 임의의 점에서 다른 직선까지의 거리는 항상 일정하다.", "O", "평행한 두 직선은 서로 만나지 않으며 그 간격이 항상 일정하게 유지된다.", ["평행선 사이의 거리의 정의를 다시 한번 확인한다."], "평행선 사이의 거리", STD_01_03),
    makeOx("서로 다른 세 직선이 한 점에서 만날 때, 이 세 직선으로 둘러싸인 삼각형은 존재하지 않는다.", "O", "세 직선이 한 점에서 만나면 교점이 1개뿐이므로 3개의 꼭짓점이 필요한 삼각형이 만들어지지 않는다.", ["삼각형이 만들어지기 위한 세 직선의 위치 관계 조건을 고려한다."], "세 직선과 삼각형", STD_01_02),
    makeOx("이차방정식 $x^2+y^2+Ax+By+C=0$은 $A, B, C$의 값에 관계없이 항상 원을 나타낸다.", "X", "표준형으로 바꾸었을 때 $A^2+B^2-4C > 0$이어야 반지름이 존재하여 원을 나타낸다.", ["완전제곱식으로 변환했을 때, 우변의 상수가 항상 0보다 커야 반지름이 존재함을 확인한다."], "원의 방정식 성립 조건", STD_01_04),
    makeOx("원과 직선이 서로 다른 두 점에서 만나면, 원의 중심에서 직선까지의 거리는 원의 반지름보다 길다.", "X", "서로 다른 두 점에서 만나려면 원의 중심에서 직선까지의 거리($d$)가 반지름($r$)보다 짧아야 한다 ($d < r$).", ["중심에서 직선까지의 거리($d$)와 반지름($r$)의 대소 관계에 따른 위치 관계를 점검한다."], "원과 직선의 위치 관계", STD_01_05),
    makeOx("원 $x^2+y^2=r^2$ 위의 점 $(x_1, y_1)$에서의 접선의 방정식은 $x_1x + y_1y = r^2$이다.", "O", "중심이 원점인 원 위의 점에서의 접선 공식이다.", ["중심이 원점인 원 위의 한 점이 주어졌을 때의 접선의 방정식 공식을 상기한다."], "원 위의 점에서의 접선의 방정식", STD_01_05),
    makeOx("원의 외부에 있는 한 점에서 그 원에 그을 수 있는 접선은 오직 한 개이다.", "X", "원 외부의 한 점에서는 항상 두 개의 접선을 그을 수 있다.", ["원 외부의 한 점을 지나면서 원에 접하는 직선을 기하학적으로 그려서 개수를 확인한다."], "원 밖의 점에서의 접선", STD_01_05),
    makeOx("중심이 직선 $y=x$ 위에 있는 원의 방정식은 항상 $x$축과 $y$축에 동시에 접한다.", "X", "중심이 $(a, a)$이더라도 반지름이 $|a|$와 같지 않으면 두 축에 동시에 접하지 않는다.", ["x축과 y축에 동시에 접하려면 중심 좌표 (a,a)와 반지름 r 사이에 |a|=r 조건이 추가로 필요함을 이해한다."], "축에 접하는 원", STD_01_04),
    makeOx("반지름의 길이가 같은 두 원은 서로 평행이동하여 완전히 포갤 수 있다.", "O", "반지름이 같은 두 원은 크기와 모양이 같은 합동이므로, 중심의 위치만 이동하면 완전히 포개어진다.", ["원은 중심의 위치와 반지름의 길이로 결정되며, 반지름이 같으면 모양과 크기가 합동임을 이용한다."], "원의 평행이동", STD_01_06),
    makeOx("직선 $y=mx+n$을 $x$축 방향으로 $a$만큼, $y$축 방향으로 $b$만큼 평행이동해도 직선의 기울기는 변하지 않는다.", "O", "평행이동은 도형의 위치만 바꿀 뿐 기울기나 모양을 변화시키지 않는다.", ["평행이동은 도형의 위치만 바꿀 뿐 기울기나 모양을 변화시키지 않음을 기억한다."], "직선의 평행이동", STD_01_06),
    makeOx("어떤 도형을 원점에 대하여 대칭이동한 후, 다시 $y$축에 대하여 대칭이동한 결과는 $x$축에 대하여 대칭이동한 결과와 같다.", "O", "원점 대칭은 $(-x,-y)$, 다시 $y$축 대칭은 $(x, -y)$가 되므로 이는 처음부터 $x$축 대칭한 것과 동일하다.", ["점 (x,y)의 좌표 변환 과정을 단계별로 추적하여 부호 변화를 확인한다."], "대칭이동의 합성", STD_01_07),
    makeOx("도형 $f(x, y)=0$을 직선 $y=x$에 대하여 대칭이동한 도형의 방정식은 $f(-y, -x)=0$이다.", "X", "직선 $y=x$ 대칭이동은 $x$와 $y$의 위치만 교환하여 $f(y, x)=0$이 된다.", ["직선 y=x 대칭이동은 x와 y의 위치를 교환하며, 부호까지 바꾸는 것은 직선 y=-x 대칭임을 구별한다."], "직선 y=x에 대한 대칭이동", STD_01_07),
    makeOx("원을 임의의 직선에 대하여 대칭이동하면 원의 중심의 위치는 항상 바뀐다.", "X", "대칭축이 되는 직선이 원의 중심을 지날 경우, 원은 그 자리에서 대칭되므로 중심이 바뀌지 않는다.", ["대칭축이 되는 직선이 원의 중심을 지날 경우, 대칭이동 후에도 중심이 변하지 않는 예외를 고려한다."], "원의 대칭이동", STD_01_07),
    makeOx("두 점 $A, B$가 직선 $l$에 대하여 대칭일 때, 선분 $AB$의 중점은 항상 직선 $l$ 위에 있다.", "O", "선대칭의 정의에 따라 선분 $AB$는 직선 $l$에 의해 수직이등분되므로 그 중점은 교점인 $l$ 위에 있다.", ["선대칭 도형의 성질에 따라 두 점을 이은 선분이 대칭축에 의해 수직이등분됨을 이용한다."], "선대칭의 성질", STD_01_07),
    makeOx("점 $(x, y)$를 $x$축 방향으로 $a$만큼 평행이동할 때는 $(x+a, y)$가 되지만, 도형 $f(x,y)=0$을 평행이동할 때는 $f(x-a, y)=0$으로 대입해야 한다.", "O", "점의 이동은 직접 더하고, 도형의 방정식은 이동 부호를 반대로 하여 대입한다.", ["점의 평행이동 규칙과 자취의 방정식에 대입하는 도형의 평행이동 규칙의 차이점을 점검한다."], "도형의 평행이동 규칙", STD_01_06),
    makeOx("점 $(a, b)$를 $x$축에 대하여 대칭이동한 점을 다시 원점에 대하여 대칭이동한 결과는 원래 점을 $y$축에 대하여 대칭이동한 점과 같다.", "O", "$(a, b) \\rightarrow (a, -b) \\rightarrow (-a, b)$가 되며, 이는 원래 점의 $x$부호만 바꾼 $y$축 대칭과 같다.", ["x축 대칭과 원점 대칭을 연속으로 수행했을 때 각 좌표의 부호가 어떻게 최종적으로 변하는지 추적한다."], "대칭이동의 합성", STD_01_07),

    // --- [동적 생성 문제 5종] ---
    (function() {
      const m1 = riSafe(2, 5);
      const isO = Math.random() < 0.5;
      const m2_num = isO ? -1 : riSafe(-3, 1);
      const m2_den = isO ? m1 : (m2_num===0?1:riSafe(2,4));
      const m2_str = toFrac(m2_num, m2_den);
      const m2_val = m2_num / m2_den;
      const ans = (Math.abs(m1 * m2_val - (-1)) < 0.001) ? "O" : "X";
      return makeOx(`두 직선 $y = ${m1}x + 3$과 $y = ${m2_str}x + 1$은 서로 수직이다.`, ans, `두 기울기의 곱은 $${m1} \\times (${m2_str})$이므로 $-1$이 ${ans==="O"?"된다":"되지 않는다"}.`, ["두 직선이 수직일 조건은 두 기울기의 곱이 -1이 되는지 계산하여 확인한다."], "직선의 수직 판별", STD_01_02);
    })(),
    (function() {
      const x1 = riSafe(-3, 3), y1 = riSafe(-3, 3);
      const a = 3, b = -4, c = riSafe(-5, 5);
      const real_d = Math.abs(a*x1 + b*y1 + c) / 5;
      const isO = Math.random() < 0.5;
      const d_show = isO ? real_d : real_d + riSafe(1, 3);
      const ans = (real_d === d_show) ? "O" : "X";
      return makeOx(`점 $(${x1}, ${y1})$에서 직선 $${a}x ${spNZ(b)}y ${spNZ(c)}=0$까지의 거리는 $${d_show}$이다.`, ans, `거리 공식에 대입하면 $d = \\dfrac{|${a}(${x1}) ${spNZ(b)}(${y1}) ${spNZ(c)}|}{\\sqrt{3^2+(-4)^2}} = ${real_d}$이다.`, ["점과 직선 사이의 거리 공식에 대입하여 직접 연산한다."], "점과 직선 사이의 거리 연산", STD_01_03);
    })(),
    (function() {
      const a = riSafe(-3, 3), b = riSafe(-3, 3), r = riSafe(2, 4);
      const isO = Math.random() < 0.5;
      const C_real = a*a + b*b - r*r;
      const C_show = isO ? C_real : C_real + riSafe(1, 5);
      const ans = (C_real === C_show) ? "O" : "X";
      return makeOx(`원 $x^2 + y^2 ${spNZ(-2*a)}x ${spNZ(-2*b)}y ${spNZ(C_show)} = 0$의 반지름의 길이는 $${r}$이다.`, ans, `표준형으로 고치면 $(x${spNZ(-a)})^2 + (y${spNZ(-b)})^2 = ${a*a+b*b-C_show}$ 이고, 우변이 $${r*r}$이 ${ans==="O"?"맞다":"아니다"}.`, ["주어진 원의 방정식을 표준형으로 변환하여 우변의 값을 확인한다."], "원의 반지름 구하기", STD_01_04);
    })(),
    (function() {
      const x = riSafe(1, 4), y = riSafe(1, 4), dx = riSafe(1, 3), dy = riSafe(-3, -1);
      const px = x + dx, py = y + dy;
      const rx = py, ry = px; // y=x 대칭이동
      const isO = Math.random() < 0.5;
      const show_x = isO ? rx : -rx;
      const show_y = isO ? ry : ry + 1;
      const ans = (show_x === rx && show_y === ry) ? "O" : "X";
      return makeOx(`점 $(${x}, ${y})$를 $x$축 방향으로 $${dx}$, $y$축 방향으로 $${dy}$만큼 평행이동한 후, 직선 $y=x$에 대하여 대칭이동한 점의 좌표는 $(${show_x}, ${show_y})$이다.`, ans, `평행이동한 점은 $(${px}, ${py})$이고 $y=x$ 대칭이동하면 $(${rx}, ${ry})$가 된다.`, ["점에 이동량을 먼저 더한 후, 대칭이동 규칙에 따라 x와 y의 자리를 교환하여 순차적으로 구한다."], "이동의 복합 연산", STD_01_07);
    })(),
    (function() {
      const r = riSafe(2, 5);
      const k = riSafe(r-2, r+2);
      const d = Math.abs(k);
      const condition = Math.random() < 0.5 ? "서로 다른 두 점에서 만난다" : "만나지 않는다";
      let ans = "X";
      if (condition === "서로 다른 두 점에서 만난다" && d < r) ans = "O";
      if (condition === "만나지 않는다" && d > r) ans = "O";
      return makeOx(`원 $x^2 + y^2 = ${r*r}$와 직선 $x = ${k}$는 ${condition}.`, ans, `중심에서 직선까지 거리는 $${d}$이고 반지름은 $${r}$이므로 교점이 ${d<r?"2개":"2개가 아니다"}.`, ["원의 반지름과 중심에서 직선까지의 거리를 비교하여 교점의 존재 여부를 판단한다."], "원과 직선의 위치 관계 판별", STD_01_05);
    })()
  ];

  // --- [Lv3 표준 단계 OX 뱅크: 정적 20문항 + 동적 5문항] ---
  const lv3_OX = [
    makeOx("선분 $AB$를 $2:1$로 외분하는 점을 $C$라 할 때, 점 $B$는 선분 $AC$의 중점이다.", "O", "수직선 위에 $A, B, C$ 순서로 위치하며 $AB=BC$이므로 점 $B$는 $AC$의 중점이다.", ["외분점의 정의에 따라 세 점 A, B, C의 위치 관계와 거리의 비를 수직선에 그려 확인한다."], "외분점의 기하학적 의미", STD_01_01),
    makeOx("좌표평면 위의 세 점 $A, B, C$에 대하여 $AB^2 + BC^2 = AC^2$이 성립하면, 삼각형 $ABC$는 $\\angle B = 90^\\circ$인 직각삼각형이다.", "O", "피타고라스 정리의 역에 의해 빗변이 $AC$인 직각삼각형이 된다.", ["피타고라스 정리의 역을 이용하여 빗변이 어느 선분인지 파악한다."], "직각삼각형의 조건", STD_01_01),
    makeOx("네 점 $A, B, C, D$를 꼭짓점으로 하는 사각형 $ABCD$가 평행사변형일 필요충분조건은 두 대각선 $AC$와 $BD$의 중점이 일치하는 것이다.", "O", "평행사변형의 두 대각선은 서로를 이등분한다는 성질과 동치이다.", ["평행사변형의 대각선은 서로를 이등분한다는 기하학적 성질을 좌표평면의 중점 공식과 연결한다."], "평행사변형의 좌표 조건", STD_01_01),
    makeOx("두 직선 $ax+by+c=0$과 $a'x+b'y+c'=0$의 교점을 지나는 모든 직선의 방정식은 실수 $k$에 대하여 $(ax+by+c)+k(a'x+b'y+c')=0$ 꼴로 나타낼 수 있다.", "X", "이 식으로는 기준이 되는 직선 $a'x+b'y+c'=0$ 자체를 나타낼 수 없으므로 '모든 직선'이라는 표현은 틀렸다.", ["위 방정식은 k값에 관계없이 교점을 지나지만, 직선 a'x+b'y+c'=0 그 자체는 k로 나타낼 수 없다는 한계를 기억한다."], "두 직선의 교점을 지나는 직선", STD_01_02),
    makeOx("직선 $y=mx+n$이 $x$축, $y$축과 만나는 점을 각각 $P, Q$라 할 때, 원점에서 선분 $PQ$에 내린 수선의 길이는 $\\dfrac{|n|}{\\sqrt{m^2+1}}$이다.", "O", "원점 $(0,0)$에서 직선 $mx-y+n=0$까지의 거리 공식을 대입한 결과와 같다.", ["원점에서 직선 mx-y+n=0까지의 거리를 점과 직선 사이의 거리 공식으로 도출한다."], "수선의 길이와 점직거리", STD_01_03),
    makeOx("두 직선이 서로 평행하면, 한 직선 위의 임의의 두 점과 다른 직선 위의 한 점으로 만든 삼각형의 넓이는 세 점의 위치에 관계없이 항상 일정하다.", "X", "평행선 사이의 거리(높이)는 일정하지만, 한 직선 위에 잡은 두 점 사이의 거리(밑변)가 변하면 넓이도 변한다.", ["평행선 사이의 높이는 일정하지만, 한 직선 위에 잡은 두 점 사이의 거리(밑변)가 변하면 넓이도 변한다."], "평행선과 삼각형의 넓이", STD_01_03),
    makeOx("원 $(x-a)^2 + (y-b)^2 = r^2$ 위의 점과 외부의 한 점 $P$ 사이의 거리의 최댓값은 점 $P$에서 원의 중심까지의 거리에 반지름 $r$을 더한 값이다.", "O", "거리의 최댓값은 중심을 관통하여 반대편 원 위 점까지의 거리이므로 $d+r$이다.", ["원의 중심, 원 위의 점, 외부의 점이 일직선상에 놓일 때 최대/최소가 발생함을 기하학적으로 연상한다."], "원과 점 사이의 최대 거리", STD_01_04),
    makeOx("두 원이 한 점에서 만나 접할 때, 두 원의 중심 사이의 거리는 항상 두 원의 반지름의 길이의 합과 같다.", "X", "외접할 때는 반지름의 합과 같지만, 내접할 때는 반지름의 차와 같다.", ["외접할 때는 반지름의 합과 같지만, 내접할 때는 반지름의 차와 같음을 구별한다."], "두 원의 접할 조건", STD_01_05),
    makeOx("점 $(x_1, y_1)$이 원 $x^2+y^2=r^2$의 내부에 있기 위한 필요충분조건은 $x_1^2+y_1^2 < r^2$이다.", "O", "원점에서 점까지의 거리의 제곱이 반지름의 제곱보다 작아야 원 내부에 존재한다.", ["원점과 점 사이의 거리가 반지름보다 작아야 원 내부에 위치함을 부등식으로 표현한다."], "원의 내부 조건", STD_01_04),
    makeOx("직선 $y=mx+n$이 원 $x^2+y^2=r^2$에 접하기 위한 조건은 $n^2 = r^2(m^2+1)$이다.", "O", "중심 $(0,0)$에서 직선 $mx-y+n=0$까지의 거리 $d$가 $r$과 같음을 양변 제곱하여 정리한 식이다.", ["중심 (0,0)에서 직선 mx-y+n=0까지의 거리 d가 반지름 r과 같다는 식의 양변을 제곱하여 정리한다."], "원과 직선의 접할 조건", STD_01_05),
    makeOx("두 원의 방정식의 이차항을 서로 소거하여 얻은 일차방정식은 항상 두 원의 공통현을 지나는 직선의 방정식을 나타낸다.", "X", "두 원이 만나지 않아 공통현이 존재하지 않을 때도 이 식은 그려지므로 항상 공통현을 지나는 직선을 나타내는 것은 아니다.", ["두 원이 서로 만나지 않으면 공통현 자체가 존재하지 않으므로, 이 식이 항상 공통현을 의미하는 것은 아니다."], "공통현의 방정식 예외", STD_01_05),
    makeOx("좌표평면 위에서 $x$축과 $y$축에 동시에 접하는 원의 중심은 항상 직선 $y=x$ 또는 $y=-x$ 위에 존재한다.", "O", "동시에 접하려면 중심 좌표의 절댓값이 $|a|=|b|$여야 하므로 두 직선 중 하나 위에 놓이게 된다.", ["두 축에 동시에 접하면 중심의 x좌표와 y좌표의 절댓값이 같아야 함을 이용한다."], "축에 동시 접하는 원의 중심", STD_01_04),
    makeOx("도형을 $x$축 방향으로 $a$, $y$축 방향으로 $b$만큼 평행이동한 후 원점에 대하여 대칭이동한 결과는, 원점에 대하여 대칭이동한 후 $x$축 방향으로 $-a$, $y$축 방향으로 $-b$만큼 평행이동한 결과와 일치한다.", "O", "두 변환 순서를 각각 적용하면 최종적으로 $(-x-a, -y-b)$가 되어 결과가 동일하다.", ["임의의 점 (x, y)에 두 가지 변환 순서를 각각 적용하여 최종 좌표가 같은지 증명한다."], "평행이동과 원점 대칭의 합성", STD_01_07),
    makeOx("점 $(a, b)$를 직선 $y=x$에 대하여 대칭이동한 점과 $x$축에 대하여 대칭이동한 점은 항상 원점에 대하여 서로 대칭이다.", "X", "$(b, a)$와 $(a, -b)$는 $a=b$ 또는 $a=-b$ 같은 특수한 경우가 아니면 원점 대칭이 아니다.", ["(b, a)와 (a, -b)가 원점 대칭이 되려면 특정한 조건이 필요하며, 항상 성립하는 일반적 성질이 아님을 파악한다."], "직선 대칭과 축 대칭", STD_01_07),
    makeOx("도형 $f(x, y)=0$을 $f(y, x)=0$으로 바꾸는 변환은 도형을 원점을 중심으로 $90^\\circ$ 회전하는 변환과 같다.", "X", "이는 회전 변환이 아니라 직선 $y=x$에 대한 선대칭 변환이다.", ["f(y,x)=0은 직선 y=x에 대한 선대칭 변환이지 회전 변환이 아님을 명확히 구분한다."], "직선 y=x 대칭 변환", STD_01_07),
    makeOx("평행이동 $(x, y) \\rightarrow (x+a, y+b)$에 의하여 포물선 $y=x^2$의 꼭짓점은 항상 $(a, b)$로 이동한다.", "O", "기본 포물선의 꼭짓점 $(0,0)$이 이동 규칙에 따라 $(a, b)$가 된다.", ["기본 포물선의 꼭짓점 (0,0)에 평행이동 규칙을 적용한다."], "포물선의 평행이동", STD_01_06),
    makeOx("두 원의 반지름이 다르면, 한 원을 평행이동 및 대칭이동하여 다른 원과 완전히 포갤 수 있는 방법은 존재하지 않는다.", "O", "평행이동과 대칭이동은 크기를 변화시키지 않는 합동 변환이기 때문이다.", ["평행이동과 대칭이동은 크기를 변화시키지 않는 합동 변환임을 상기한다."], "합동 변환과 원", STD_01_06),
    makeOx("도형 $f(-x, y)=0$은 도형 $f(x, -y)=0$을 원점에 대하여 대칭이동한 도형이다.", "O", "$f(x, -y)=0$에 원점 대칭 규칙인 $x \\rightarrow -x, y \\rightarrow -y$를 적용하면 $f(-x, -(-y))=f(-x, y)$가 된다.", ["f(x, -y)=0에 원점 대칭 변환 x->-x, y->-y를 대입하여 f(-x, y)=0이 나오는지 확인한다."], "대칭 변환의 적용", STD_01_07),
    makeOx("중심이 원점인 원을 원점에 대하여 대칭이동하면 원의 방정식은 변하지 않는다.", "O", "$(-x)^2+(-y)^2=r^2$은 $x^2+y^2=r^2$과 완전히 동일한 식이다.", ["방정식 x^2+y^2=r^2에 x 대신 -x, y 대신 -y를 대입해도 식의 형태가 동일함을 확인한다."], "원점 대칭의 불변성", STD_01_07),
    makeOx("직선 $ax+by+c=0$을 직선 $y=x$에 대하여 대칭이동한 직선의 기울기는 항상 원래 직선의 기울기의 역수이다.", "X", "수평선이나 수직선의 경우 기울기가 $0$이거나 정의되지 않아 역수 관계가 항상 성립하는 것은 아니다.", ["수평선(y=k)이나 수직선(x=k)의 경우 기울기가 0이거나 정의되지 않아 역수 관계가 항상 성립하지 않는 예외를 고려한다."], "직선의 대칭이동과 기울기", STD_01_07),

    // --- [동적 생성 문제 5종] ---
    (function() {
      const x1 = riSafe(-3, 3), y1 = riSafe(-3, 3);
      const x2 = riSafe(-3, 3), y2 = riSafe(-3, 3);
      const x3 = riSafe(-3, 3), y3 = riSafe(-3, 3);
      const sumX = x1+x2+x3, sumY = y1+y2+y3;
      const gxStr = sumX%3 === 0 ? `${sumX/3}` : `${sumX}/3`;
      const gyStr = sumY%3 === 0 ? `${sumY/3}` : `${sumY}/3`;
      const isO = Math.random() < 0.5;
      const show_gx = isO ? gxStr : `${(sumX+riSafe(1,2))}/3`;
      const show_gy = isO ? gyStr : `${(sumY+riSafe(1,2))}/3`;
      const ans = isO ? "O" : "X";
      return makeOx(`세 점 $A(${x1}, ${y1}), B(${x2}, ${y2}), C(${x3}, ${y3})$을 꼭짓점으로 하는 삼각형 $ABC$의 각 변의 중점을 연결하여 만든 삼각형의 무게중심의 좌표는 $(${show_gx}, ${show_gy})$이다.`, ans, `중점을 연결한 삼각형의 무게중심은 원래 삼각형의 무게중심 $(${gxStr}, ${gyStr})$과 동일하다.`, ["원래 삼각형의 무게중심과 각 변의 중점을 이은 삼각형의 무게중심은 항상 일치한다는 성질을 이용하여 세 좌표의 평균을 구한다."], "중점 삼각형의 무게중심", STD_01_01);
    })(),
    (function() {
      const sets = [[3,4,5], [5,12,13], [6,8,10]];
      const s = sets[riSafe(0, 2)];
      const a = s[0], b = s[1], hyp = s[2];
      const dist = riSafe(1, 4);
      const real_k = dist * hyp;
      const isO = Math.random() < 0.5;
      const show_k = isO ? real_k : real_k + riSafe(1, 4);
      const ans = isO ? "O" : "X";
      return makeOx(`원점 $(0,0)$에서 직선 $${a}x-${b}y+k=0$까지의 거리가 $${dist}$일 때, 양수 $k$의 값은 $${show_k}$이다.`, ans, `거리 공식에 대입하면 $\\dfrac{k}{\\sqrt{${a}^2+(-${b})^2}} = \\dfrac{k}{${hyp}} = ${dist}$이므로 $k=${real_k}$이다.`, ["점과 직선 사이의 거리 공식에 대입하여 절댓값 방정식을 풀고 양수 조건을 확인한다."], "거리 공식을 이용한 미정계수", STD_01_03);
    })(),
    (function() {
      const r = riSafe(2, 4);
      const a = riSafe(1, 4), b = riSafe(1, 4), c = riSafe(-5, -1);
      const r2 = r*r;
      const isO = Math.random() < 0.5;
      // 일반형: x^2+y^2 - 2ax - 2by + c = 0
      // x^2+y^2 = r2
      // 빼면: 2ax + 2by - c - r2 = 0
      const constTerm = -c - r2;
      const show_const = isO ? constTerm : constTerm + riSafe(1, 4);
      const ans = (constTerm === show_const) ? "O" : "X";
      return makeOx(`두 원 $x^2+y^2=${r2}$와 $x^2+y^2-${2*a}x-${2*b}y+${c}=0$의 공통현을 지나는 직선의 방정식은 $${2*a}x+${2*b}y${spNZ(show_const)}=0$이다.`, ans, `두 식의 이차항을 소거하기 위해 서로 빼면 $${2*a}x+${2*b}y${spNZ(constTerm)}=0$이 된다.`, ["두 원의 교점을 지나는 직선(공통현)의 방정식은 두 원의 이차항을 서로 빼서 소거하여 구한다."], "공통현의 방정식 연산", STD_01_05);
    })(),
    (function() {
      const r = riSafe(2, 4);
      const a = 3, b = 4;
      const c = riSafe(10, 25);
      const d = c / 5; // 거리
      const real_max = d + r;
      const isO = Math.random() < 0.5;
      const show_max = isO ? real_max : d - r; // 오답으로 최솟값 등을 줌
      const ans = isO ? "O" : "X";
      return makeOx(`원 $x^2+y^2=${r*r}$ 위의 점과 직선 $3x+4y-${c}=0$ 사이의 거리의 최댓값은 $${show_max}$이다.`, ans, `원점에서 직선까지 거리는 $d=${d}$이고, 거리의 최댓값은 $d+r = ${d}+${r} = ${real_max}$이다.`, ["거리의 최댓값은 원의 중심에서 직선까지의 거리에 원의 반지름을 더한 값임을 이용하여 기하학적으로 연산한다."], "원 위의 점과 직선 사이의 최대 거리", STD_01_05);
    })(),
    (function() {
      const ax = riSafe(-2, 2), ay = riSafe(-2, 2); // 중심 M(ax, ay)
      const px = riSafe(-4, 4), py = riSafe(-4, 4); // 점 P(px, py)
      const rx = 2*ax - px, ry = 2*ay - py;
      const isO = Math.random() < 0.5;
      const show_rx = isO ? rx : rx + riSafe(1, 3);
      const show_ry = isO ? ry : ry - riSafe(1, 3);
      const ans = (show_rx === rx && show_ry === ry) ? "O" : "X";
      return makeOx(`점 $(${px}, ${py})$를 점 $(${ax}, ${ay})$에 대하여 대칭이동한 점의 좌표는 $(${show_rx}, ${show_ry})$이다.`, ans, `중점 공식을 역으로 적용하면 $(2(${ax})-${px}, 2(${ay})-${py}) = $ $(${rx}, ${ry})$이다.`, ["점 P와 대칭이동된 점의 중점이 기준점이 되어야 함을 이용하여 각 좌표별로 중점 공식을 역산한다."], "점에 대한 대칭이동", STD_01_07);
    })()
  ];

  // --- [Lv4 심화 단계 OX 뱅크: 정적 20문항 + 동적 5문항] ---
  const lv4_OX = [
    makeOx("두 도시 $A, B$가 좌표평면 상의 점 $(-3, 0), (3, 0)$에 위치할 때, 두 도시로부터 거리의 비가 $2:1$인 지점들을 연결하여 순환도로를 건설하면 이 도로는 완전한 원 모양이 된다.", "O", "두 점으로부터 거리의 비가 일정한 점들의 자취는 아폴로니우스의 원이 되기 때문이다.", ["두 점으로부터 거리의 비가 m:n (m!=n)인 점들의 자취는 '아폴로니우스의 원'이 됨을 상기한다."], "아폴로니우스의 원", STD_01_04),
    makeOx("강가(직선 $y=x$)를 따라 이동하는 물류 로봇이 공장 $A(2, 5)$에서 물건을 싣고 강가의 한 지점 $P$를 거쳐 창고 $B(6, 1)$로 갈 때, 이동 거리의 최솟값을 구하기 위해 공장 $A$를 강가(직선 $y=x$)에 대칭이동시킨 점을 활용할 수 있다.", "O", "선분 $AP+PB$의 최솟값은 $A$를 대칭이동한 점 $A'$와 $B$를 잇는 일직선 거리를 통해 구할 수 있다.", ["꺾인 선분의 최단 거리는 대칭이동을 통해 일직선으로 펼쳐서 구하는 기하학적 원리를 적용한다."], "대칭이동을 활용한 최단 거리", STD_01_07),
    makeOx("세 점 $A, B, C$가 일직선 위에 있지 않을 때, 임의의 점 $P$에 대하여 $PA^2+PB^2+PC^2$의 값을 최소로 만드는 점 $P$의 위치는 삼각형 $ABC$의 외심이다.", "X", "해당 식의 값을 최소로 만드는 점 $P$의 위치는 외심이 아니라 삼각형 $ABC$의 무게중심이다.", ["점 P의 좌표를 (x,y)로 두고 이차함수의 최솟값을 구하는 방식으로 정리하면, 점 P는 외심이 아닌 무게중심이 됨을 도출할 수 있다."], "제곱합의 최솟값과 무게중심", STD_01_01),
    makeOx("좌표평면에서 원점이 아닌 세 점 $(0,0), (a, b), (c, d)$를 꼭짓점으로 하는 삼각형의 넓이는 $\\dfrac{1}{2}|ad-bc|$로 계산할 수 있다.", "O", "원점과 두 점을 꼭짓점으로 하는 삼각형에 사선 공식을 적용한 기본 형태이다.", ["세 점의 좌표가 주어졌을 때 사선 공식(신발끈 공식)을 원점을 기준으로 적용해 본다."], "원점을 포함하는 삼각형의 넓이", STD_01_03),
    makeOx("두 직선 $a_1x+b_1y+c_1=0$과 $a_2x+b_2y+c_2=0$이 이루는 각을 이등분하는 직선의 방정식은, 두 직선으로부터의 거리가 서로 같은 점들의 자취의 방정식과 완전히 동일하다.", "O", "각의 이등분선 위의 임의의 점은 두 직선까지의 거리가 항상 같다는 기하학적 성질과 동치이다.", ["각의 이등분선 위의 임의의 점은 두 반직선(직선)까지 이르는 거리가 항상 같다는 기하학적 정의를 활용한다."], "각의 이등분선의 자취", STD_01_03),
    makeOx("폭이 일정한 두 직선 모양의 도로가 비스듬히 교차할 때, 두 도로가 이루는 각의 이등분선 위를 걷는 사람은 양쪽 도로의 경계선으로부터 항상 같은 거리를 유지하게 된다.", "O", "각의 이등분선의 자취의 성질이 도로의 경계선 모델에 그대로 적용된다.", ["각의 이등분선의 자취의 성질이 실생활 공간의 직선 사이의 거리에 어떻게 적용되는지 연상한다."], "각의 이등분선의 실생활 적용", STD_01_03),
    makeOx("세 직선 $l_1, l_2, l_3$가 좌표평면 위에서 삼각형을 이루지 않을 조건은 '세 직선 중 어느 두 직선이 평행한 경우'와 '세 직선이 한 점에서 만나는 경우' 두 가지뿐이다.", "O", "평면 위에서 세 직선이 교차하여 3개의 꼭짓점을 형성하지 못하는 모든 위치 관계이다.", ["평면 위에서 세 직선이 교차하여 삼각형의 세 꼭짓점을 형성하지 못하는 위치 관계를 모두 나열해 본다."], "삼각형을 이루지 않을 조건", STD_01_02),
    makeOx("원 $x^2+y^2=r^2$에 외접하면서 반지름의 길이가 $R$인 원의 중심이 그리는 자취는 반지름의 길이가 $r+R$인 원이다.", "O", "외접하는 두 원의 중심 사이의 거리가 $r+R$로 항상 일정하므로 이를 반지름으로 하는 동심원이 된다.", ["외접하는 두 원의 중심 사이의 거리는 두 반지름의 합과 같다는 성질을 이용하여 자취 방정식을 세운다."], "외접원의 중심의 자취", STD_01_05),
    makeOx("두 원 $x^2+y^2=4$와 $(x-4)^2+(y-3)^2=r^2$이 서로 만나지 않기 위한 양수 $r$의 값의 범위는 $r < 3$ 또는 $r > 7$이다.", "O", "중심 거리가 $5$이고 반지름이 $2, r$이므로, $5 > 2+r$ 또는 $5 < |2-r|$을 풀면 성립한다.", ["중심 거리가 5임을 구하고, 두 원이 서로 외부에 있을 조건과 내부에 있을 조건을 각각 계산한다."], "두 원의 위치 관계 연산", STD_01_05),
    makeOx("점 $P(x_1, y_1)$에서 원 $x^2+y^2=r^2$에 그은 두 접선이 서로 수직일 때, 점 $P$가 그리는 자취의 방정식은 $x^2+y^2=2r^2$이다.", "O", "두 접선이 수직이면 반지름 $r$을 변으로 하는 정사각형이 만들어져 중심에서 $P$까지 거리는 $\\sqrt{2}r$이 된다.", ["접선이 수직이면 원의 중심, 두 접점, 점 P가 이루는 사각형이 한 변의 길이가 r인 정사각형이 됨을 이용한다."], "직교하는 두 접선의 자취", STD_01_05),
    makeOx("두 송신탑의 전파 도달 범위가 각각 원형일 때, 두 전파가 겹치는 구역의 경계가 만나는 두 교점을 잇는 직선은 두 송신탑을 연결한 선분을 항상 수직으로 이등분한다.", "X", "수직으로 지나는 것은 맞으나, '이등분'하려면 두 송신탑의 도달 범위(반지름)가 정확히 같아야만 한다.", ["두 원의 공통현이 중심선을 수직으로 지나는 것은 맞으나, '이등분'을 하려면 두 송신탑의 도달 범위(반지름)가 정확히 같아야만 함을 구별한다."], "두 원의 공통현과 중심선", STD_01_05),
    makeOx("두 원이 서로 직교(두 원의 교점에서의 각각의 접선이 서로 수직)할 때, 두 원의 중심과 한 교점을 꼭짓점으로 하여 만든 삼각형은 항상 직각삼각형이다.", "O", "접선은 그 점에서의 반지름과 수직이므로 교점에서의 각도가 $90^\\circ$가 되어 직각삼각형이 된다.", ["원의 접선은 그 접점에서의 반지름과 수직이라는 성질을 바탕으로 교점에서의 각도를 추론한다."], "직교하는 두 원", STD_01_05),
    makeOx("당구대 위의 공 $A$를 쳐서 벽면에 한 번 쿠션을 맞히고 공 $B$를 맞히는 경로는, 쿠션이 되는 벽면을 대칭축으로 하여 공 $B$의 위치를 대칭이동시킨 가상의 점을 향해 공 $A$를 직접 치는 직선 경로와 기하학적으로 일치한다.", "O", "입사각과 반사각이 같은 물리 법칙은 선대칭을 이용한 최단 거리 작도 원리와 동일하다.", ["입사각과 반사각이 같다는 물리적 법칙이 선대칭을 이용한 최단 거리 작도 원리와 기하학적으로 동일함을 이해한다."], "선대칭의 물리적 응용", STD_01_07),
    makeOx("중심이 $(2, 3)$인 원을 직선 $y=x+1$에 대하여 대칭이동시켜도 원의 방정식은 전혀 변하지 않는다.", "O", "원의 중심 $(2, 3)$이 이미 대칭축인 $y=x+1$ 위에 존재하므로 대칭이동해도 제자리이다.", ["원의 중심 (2, 3)을 직선 y=x+1에 대입해 보면 성립하므로, 대칭축이 원의 중심을 관통하고 있음을 파악한다."], "대칭축 위의 원", STD_01_07),
    makeOx("어떤 도형을 점 $(a, b)$에 대하여 대칭이동하는 것은, 그 도형을 $x$축 방향으로 $a$만큼, $y$축 방향으로 $b$만큼 평행이동한 뒤 원점에 대하여 대칭이동하는 것과 완전히 동일한 변환이다.", "X", "점 $(a, b)$ 대칭이동은 $(2a-x, 2b-y)$를 대입하지만, 평행이동 후 원점 대칭은 $(-x+a, -y+b)$가 되어 다르다.", ["점 (a, b) 대칭이동은 식에 (2a-x, 2b-y)를 대입하지만, 평행이동 후 원점 대칭은 (-x+a, -y+b)를 대입하게 되어 결과가 다름을 연산해 본다."], "점대칭과 합성 변환", STD_01_07),
    makeOx("도형 $f(x, y)=0$을 $f(-y, -x)=0$으로 바꾸는 변환은 좌표평면 상에서 직선 $y=-x$에 대한 선대칭 이동을 의미한다.", "O", "$x$에 $-y$를, $y$에 $-x$를 대입하는 것은 $y=-x$ 대칭이동의 규칙이다.", ["x에 -y를, y에 -x를 대입하는 것이 어느 직선을 기준으로 접었을 때의 좌표 변화인지 규칙을 상기한다."], "직선 y=-x 대칭 변환", STD_01_07),
    makeOx("두 점 $A, B$와 원 $x^2+y^2=r^2$ 위의 점 $P$로 이루어진 삼각형 $PAB$의 넓이가 최대가 되기 위해서는, 점 $P$에서의 원의 접선이 직선 $AB$와 서로 평행해야 한다.", "O", "밑변 $AB$가 고정일 때 높이가 최대가 되려면 직선 $AB$와 가장 멀리 떨어진 평행한 접선 위의 점이어야 한다.", ["삼각형의 밑변 AB의 길이는 고정되어 있으므로, 높이가 최대가 되는 원 위의 점의 기하학적 조건을 접선의 기울기와 연결하여 생각한다."], "삼각형의 최대 넓이와 접선", STD_01_05),
    makeOx("중심이 $C(a, b)$이고 $x$축, $y$축에 동시에 접하는 원이 제1사분면에 존재할 때, 원의 중심에서 직선 $3x-4y+c=0$까지의 거리는 상수 $c$의 값에 관계없이 항상 일정하다.", "X", "중심 $(r, r)$에서 직선까지 거리는 $\\dfrac{|-r+c|}{5}$가 되어 $c$의 값에 따라 거리가 변한다.", ["제1사분면이므로 중심이 (r, r)이다. 이를 점직거리 공식에 대입하면 c의 값에 따라 거리가 변함을 확인한다."], "동시 접하는 원과 거리", STD_01_04),
    makeOx("좌표평면 상에 위치가 서로 다른 두 원 $C_1, C_2$의 넓이를 동시에 정확히 이등분하는 직선은 두 원의 중심을 동시에 지나는 직선 오직 하나뿐이다.", "O", "원의 넓이를 이등분하려면 그 중심을 지나야 하고, 서로 다른 두 점을 지나는 직선은 유일하다.", ["원의 넓이를 이등분하는 유일한 조건은 그 원의 중심을 지나는 직선이어야 함을 두 원에 동시에 적용한다."], "두 원의 넓이의 동시 이등분", STD_01_04),
    makeOx("직선 $y=mx$가 세 점 $(0,0), (4,0), (0,3)$을 꼭짓점으로 하는 삼각형의 넓이를 이등분할 때, 상수 $m$의 값은 $\\dfrac{3}{8}$이다.", "O", "삼각형 넓이 6을 3으로 나누는 교점을 구하여 기울기를 계산하면 $\\dfrac{3}{8}$이다.", ["삼각형의 전체 넓이가 6이므로, 직선이 잘라낸 삼각형의 넓이가 3이 되도록 빗변과의 교점을 구하여 계산한다."], "삼각형의 넓이 이등분", STD_01_02),

    // --- [동적 생성 문제 5종] ---
    (function() {
      const sets = [[3,4,5], [6,8,10], [5,12,13]];
      const s = sets[riSafe(0, 2)];
      const w_half = s[0]; // 반폭
      const h_real = s[1]; // 높이
      const r = s[2]; // 반지름
      const w = w_half * 2;
      const isO = Math.random() < 0.5;
      const show_h = isO ? h_real : h_real + riSafe(1, 2);
      const ans = isO ? "O" : "X";
      return makeOx(`단면이 반지름 $${r}\\text{m}$인 반원 모양의 터널이 있다. 이 터널의 중앙 선을 따라 폭이 $${w}\\text{m}$인 트럭이 통과할 때, 트럭의 높이가 $${show_h}\\text{m}$ 이하여야만 터널 천장에 닿지 않고 통과할 수 있다.`, ans, `반폭이 $${w_half}$이므로 피타고라스 정리에 의해 최대 높이는 $\\sqrt{${r}^2-${w_half}^2} = ${h_real}$이 ${ans==="O"?"된다":"아니다"}.`, ["반원 터널의 단면을 원의 방정식으로 모델링하고, x = +-(트럭 폭/2)일 때의 y값(터널 높이)을 피타고라스 정리로 계산한다."], "반원 터널과 차량 통과 높이", STD_01_04);
    })(),
    (function() {
      const x1 = riSafe(-3, 3), y1 = riSafe(-3, 3);
      const x2 = riSafe(-3, 3), y2 = riSafe(-3, 3);
      const x3 = riSafe(-3, 3), y3 = riSafe(-3, 3);
      const cross = Math.abs(x1*y2 + x2*y3 + x3*y1 - y1*x2 - y2*x3 - y3*x1);
      const S_real = cross / 2;
      const isO = Math.random() < 0.5;
      const S_show = isO ? S_real : S_real + riSafe(1, 3);
      const ans = (S_real === S_show && S_real !== 0) ? "O" : "X"; 
      return makeOx(`세 점 $A(${x1}, ${y1}), B(${x2}, ${y2}), C(${x3}, ${y3})$를 꼭짓점으로 하는 삼각형 $ABC$의 넓이는 $${S_show}$이다.`, ans, `사선 공식에 대입하면 넓이는 $\\dfrac{1}{2} |${x1*y2+x2*y3+x3*y1} - (${y1*x2+y2*x3+y3*x1})| = ${S_real}$이다.`, ["점과 직선 사이의 거리 공식을 이용해 밑변과 높이를 구하거나, 사선 공식(신발끈 공식)을 적용하여 직접 계산한다."], "세 점의 삼각형 넓이 연산", STD_01_03);
    })(),
    (function() {
      const a = riSafe(2, 5);
      const d = 3 * a; // 0과 3a 거리
      const r_real = 2 * a;
      const isO = Math.random() < 0.5;
      const r_show = isO ? r_real : r_real + riSafe(1, 3);
      const ans = isO ? "O" : "X";
      return makeOx(`두 점 $A(0, 0)$과 $B(${d}, 0)$으로부터 거리의 비가 $2:1$인 점 $P$가 그리는 자취는 원이며, 이 원의 반지름의 길이는 $${r_show}$이다.`, ans, `내분점은 $(${2*a}, 0)$, 외분점은 $(${6*a}, 0)$이므로 지름은 $${4*a}$, 반지름은 $${r_real}$이다.`, ["주어진 거리의 비로 선분 AB를 내분하는 점과 외분하는 점을 각각 구하면, 이 두 점이 만들어지는 원의 지름의 양 끝점이 됨을 이용한다."], "아폴로니우스의 원 계산", STD_01_04);
    })(),
    (function() {
      const sets = [[3,4], [6,8], [5,12]];
      const s = sets[riSafe(0, 2)];
      const dx = s[0], dy = s[1];
      const min_dist = Math.sqrt(dx*dx + dy*dy);
      const x1 = riSafe(0, 3);
      const y1 = riSafe(1, dy-1);
      const x2 = x1 + dx;
      const y2 = dy - y1; // so y1+y2 = dy
      const isO = Math.random() < 0.5;
      const show_dist = isO ? min_dist : min_dist + riSafe(1, 2);
      const ans = isO ? "O" : "X";
      return makeOx(`두 점 $A(${x1}, ${y1})$와 $B(${x2}, ${y2})$가 주어졌을 때, $x$축 위를 움직이는 임의의 점 $P$에 대하여 $AP+PB$의 최솟값은 $${show_dist}$이다.`, ans, `$A$를 $x$축 대칭한 $A'(${x1}, -${y1})$와 $B$ 사이 거리는 $\\sqrt{${dx}^2+${dy}^2} = ${min_dist}$이다.`, ["점 A를 x축에 대하여 대칭이동한 점을 A'라 할 때, 선분 A'B의 일직선 길이가 곧 AP+PB의 최솟값이 됨을 이용하여 거리를 구한다."], "대칭이동 최단 거리", STD_01_07);
    })(),
    (function() {
      const a = riSafe(2, 5), b = riSafe(2, 5);
      const isO = Math.random() < 0.5;
      const line1 = `${a}x+${b}y=0`;
      const line2 = isO ? `${b}x+${a}y=0` : `${b}x-${a}y=0`;
      const ans = isO ? "O" : "X";
      return makeOx(`두 직선 $${line1}$과 $${line2}$가 이루는 각을 이등분하는 직선 중 하나는 $y=x$이다.`, ans, `각의 이등분선 식을 세우면 $\\dfrac{|${a}x+${b}y|}{\\sqrt{${a*a+b*b}}} = \\dfrac{|${b}x${isO?"+":"-"}${a}y|}{\\sqrt{${b*b+a*a}}}$가 되어 $y=x$가 ${ans==="O"?"나온다":"나오지 않는다"}.`, ["각의 이등분선 위의 임의의 점 (x,y)에서 두 직선까지의 거리가 서로 같다는 식을 세워 방정식을 푼다."], "각의 이등분선의 방정식", STD_01_03);
    })()
  ];

  // --- [Lv5 최고 난이도 OX 뱅크: 정적 20문항 + 동적 5문항] ---
  const lv5_OX = [
    makeOx("평면 위의 임의의 네 점 $A, B, C, D$에 대하여 $AB^2 + CD^2 = AC^2 + BD^2$이 성립할 필요충분조건은 두 선분 $AD$와 $BC$가 서로 직교하는 것이다.", "O", "사각형의 두 대각선이 직교할 때 마주보는 변의 길이의 제곱의 합이 같다는 기하학적 성질의 일반화 원리를 나타낸다.", ["사각형의 두 대각선이 직교할 때 마주보는 변의 길이의 제곱의 합이 같다는 기하학적 성질의 일반화 원리를 파악한다."], "직교하는 대각선의 성질", STD_01_01),
    makeOx("10km 떨어진 두 기지국 $A, B$에서 특정 휴대폰 $P$의 전파를 수신했다. $A$에 도달한 시간이 $B$보다 2초 빠르고 전파 속력이 일정하다면, 휴대폰 $P$의 위치는 두 기지국을 초점으로 하는 쌍곡선의 일부인 곡선 위에 존재한다.", "O", "거리의 '차'가 일정한 점들의 집합은 쌍곡선이 됨을 응용한 실생활 모델링이다.", ["두 정점으로부터 거리의 '차'가 일정한 점들의 자취는 쌍곡선이 됨을 응용한 실생활 모델링이다."], "쌍곡선의 자취 응용", STD_01_01),
    makeOx("중심이 $(a, b)$인 원 내부의 한 점 $P$를 지나는 무수히 많은 현들 중에서 그 길이가 가장 짧은 현은, 점 $P$와 중심을 지나는 직선과 점 $P$에서 수직으로 만나는 현이다.", "O", "중심에서 현까지의 거리가 멀어질수록 현의 길이가 짧아지므로 수직일 때 거리가 최대가 되어 최단 현이 된다.", ["원의 중심에서 현까지의 거리가 멀어질수록 현의 길이가 짧아진다는 원의 기하학적 성질을 이용한다."], "원 내부 점의 최단 현", STD_01_04),
    makeOx("세 점을 꼭짓점으로 하는 삼각형 내부에 점 $P$가 존재할 때, 세 꼭짓점까지의 거리의 합 $PA+PB+PC$가 최소가 되게 하는 점 $P$는 삼각형의 종류에 관계없이 항상 세 내각을 각각 $120^\\circ$로 분할하는 점(페르마 포인트)이다.", "X", "둔각이 $120^\\circ$ 이상인 둔각삼각형의 경우 페르마 포인트는 둔각의 꼭짓점 자체가 되어 내각을 $120^\\circ$로 분할하지 않는다.", ["한 내각이 120도 이상인 둔각삼각형의 경우에는 페르마 포인트가 삼각형 내부가 아닌 해당 둔각의 꼭짓점 자체와 일치하는 예외가 존재한다."], "페르마 포인트의 예외", STD_01_01),
    makeOx("평면 상의 세 직선이 어느 두 직선도 서로 평행하지 않고 한 점에서도 만나지 않을 때, 이 세 직선으로부터 '같은 거리'에 있는 점은 좌표평면 상에 항상 4개 존재한다.", "O", "세 직선이 이루는 삼각형의 내부에 내심 1개, 외부에 방심 3개가 존재하여 총 4개이다.", ["세 직선이 이루는 삼각형의 내부에 존재하는 내심 1개와, 외부에 존재하는 방심 3개를 모두 고려한다."], "세 직선으로부터 같은 거리에 있는 점", STD_01_03),
    makeOx("평행한 두 고속도로 사이에 위치한 물류센터에서 두 고속도로까지 직선으로 연결한 최단 거리의 합은 물류센터의 위치에 관계없이 두 고속도로 사이의 수직 거리와 항상 일치한다.", "O", "두 평행선 사이에 위치한 점에 대하여 각각 내린 수선의 길이의 합은 두 평행선 사이의 거리와 동일하다.", ["두 평행선 사이에 위치한 점에 대하여 수선의 길이의 합이 일정함을 기하학적으로 증명한다."], "평행선 사이의 수선합", STD_01_03),
    makeOx("두 직선 $y-b=m(x-a)$와 $x-a=-m(y-b)$의 교점의 자취는 임의의 실수 $m$의 값에 관계없이 중심이 $(a, b)$인 원을 이룬다.", "X", "두 직선은 항상 서로 수직이며, 둘 다 항상 점 $(a, b)$를 지나므로 교점은 원이 아니라 오직 점 $(a, b)$ 하나뿐이다.", ["두 직선은 서로 수직이며, 둘 다 항상 점 (a, b)를 지난다. 따라서 교점은 원이 아니라 점 (a, b) 하나뿐이다."], "두 직선 교점의 자취", STD_01_02),
    makeOx("기울기가 유리수인 직선과 기울기가 무리수인 직선은 좌표평면 상에서 절대로 서로 수직으로 만날 수 없다.", "O", "두 기울기의 곱이 $-1$이 되어야 하는데, 유리수와 무리수의 곱은 $-1$이 될 수 없으므로 수직으로 만날 수 없다.", ["두 직선이 수직일 조건 m1 x m2 = -1에서 유리수와 무리수의 곱이 유리수 -1이 될 수 있는지를 귀류법으로 생각한다."], "수직인 두 직선의 기울기", STD_01_02),
    makeOx("한 평면 위의 4개의 무한 직선으로 평면을 분할할 때, 생성될 수 있는 영역의 최대 개수는 11개이다.", "O", "최대 분할 영역 개수 공식 $1 + \\frac{n(n+1)}{2}$에 $n=4$를 대입하면 $11$이 나온다.", ["n개의 직선으로 평면을 최대 분할할 때의 개수 공식을 대입하여 산출한다."], "직선에 의한 평면 분할", STD_01_02),
    makeOx("점 $P$에서 두 원 $C_1, C_2$에 그은 두 접선의 길이가 서로 같을 때, 점 $P$의 자취는 두 원의 공통현을 지나는 직선(근축)과 항상 일치한다.", "O", "외부의 한 점에서 접선의 길이가 같은 점들의 자취는 공통현을 포함하는 직선인 근축이 된다.", ["두 원의 외부에 있는 점에서 그은 접선의 길이가 같아지는 점들의 집합인 근축(Radical axis)의 성질을 상기한다."], "근축의 자취", STD_01_05),
    makeOx("반지름의 길이가 다른 두 원이 외부에서 만나지 않을 때, 두 원의 외부의 한 점에서 각 원에 그은 접선의 길이가 같아지는 점들의 자취는 존재하지 않는다.", "X", "두 원이 만나지 않아도 중심을 잇는 선분에 수직인 근축(접선의 길이가 같은 점의 자취)은 항상 1개 존재한다.", ["두 원이 겹치지 않더라도, 두 원의 중심을 잇는 선분에 수직인 근축은 항상 1개 존재한다."], "만나지 않는 두 원의 근축", STD_01_05),
    makeOx("두 원 $x^2+y^2=r_1^2$과 $(x-d)^2+y^2=r_2^2$이 서로 직교(교점에서의 두 접선이 수직)할 필요충분조건은 두 원의 중심거리 $d$에 대하여 $d^2 = r_1^2 + r_2^2$이 성립하는 것이다.", "O", "직교하는 두 원의 교점과 두 중심을 꼭짓점으로 하는 삼각형이 피타고라스 정리를 만족하는 직각삼각형이 됨을 의미한다.", ["직교하는 두 원의 교점과 두 중심을 꼭짓점으로 하는 삼각형이 직각삼각형이 됨을 피타고라스 정리로 해석한다."], "직교하는 원의 조건", STD_01_05),
    makeOx("좌표평면에서 중심이 직선 $y=mx+n$ ($n \\neq 0$) 위에 있고 두 좌표축에 동시에 접하는 원은 실수 $m$의 값에 관계없이 항상 최대 4개까지 존재할 수 있다.", "X", "좌표축에 동시에 접하려면 중심이 $y=x$ 또는 $y=-x$ 위에 있어야 하는데 일차함수 직선과 두 직선의 교점은 최대 2개뿐이다.", ["중심이 두 좌표축에 동시에 접하려면 직선 y=x 또는 y=-x 위에 있어야 한다. 일차함수 직선과의 교점은 최대 2개뿐이다."], "동시 접하는 원의 개수", STD_01_04),
    makeOx("원의 내부에 있는 임의의 점 $P(x_1, y_1)$을 지나는 원의 무수히 많은 현의 중점의 자취는 원의 중심과 점 $P$를 지름의 양 끝점으로 하는 새로운 원이다.", "O", "중심과 현의 중점을 이은 선분은 현과 수직이므로 원주각이 $90^\\circ$인 원이 자취가 된다.", ["원의 중심에서 현의 중점을 이은 선분은 항상 현과 수직이므로, 중심과 중점, 점 P가 이루는 각이 항상 직각임을 파악한다."], "현의 중점의 자취", STD_01_05),
    makeOx("어떤 도형을 서로 교차하는 두 직선 $y=mx+n$과 $y=m'x+n'$에 대하여 차례로 대칭이동시킨 결과가 원래 도형을 어떤 점에 대하여 점대칭시킨 결과와 일치하려면, 두 직선은 반드시 서로 수직이어야 한다.", "O", "두 교차하는 직선에 대한 연속 대칭이동은 교점을 중심으로 각도의 2배만큼 회전하며, 점대칭은 $180^\\circ$ 회전이므로 두 직선이 $90^\\circ$ 수직이어야 한다.", ["두 교차하는 직선에 대한 연속 대칭이동은 교점을 중심으로 두 직선 사이의 각도의 2배만큼 회전하는 변환과 같음을 응용한다."], "연속 선대칭과 점대칭", STD_01_07),
    makeOx("직사각형 방의 네 벽면을 모두 한 번씩 쿠션으로 맞히고 처음 위치로 돌아오는 레이저 포인터의 닫힌 최단 경로는, 방의 가로가 $W$, 세로가 $H$일 때 전체 길이가 항상 $\\sqrt{(2W)^2+(2H)^2}$가 된다.", "O", "빛의 반사 원리를 선대칭 이동으로 4번 펼쳐서 얻어지는 가상의 $2W \\times 2H$ 직사각형의 빗변 길이와 같다.", ["빛의 반사 원리를 선대칭 이동으로 4번 펼쳤을 때 생기는 가상의 큰 직사각형의 빗변 길이를 계산한다."], "닫힌 경로의 최단 거리", STD_01_07),
    makeOx("점 $(x_1, y_1)$을 임의의 직선 $ax+by+c=0$에 대하여 대칭이동시킨 점의 좌표를 구하는 과정은, 대칭축까지의 거리가 같다는 '거리 조건' 하나만으로는 유일하게 결정할 수 없다.", "O", "거리가 같은 점은 직선 반대편뿐만 아니라 직선과 평행한 선상에 무수히 존재하므로 반드시 '수직 조건'이 있어야 유일해진다.", ["거리가 같은 점은 직선을 기준으로 반대편 외에 무수히 존재하므로 반드시 '수직 조건'이 병행되어야 함을 확인한다."], "선대칭의 수직 조건", STD_01_07),
    makeOx("임의의 삼각형을 평행이동과 회전이동만 사용하여 한 직선에 대한 선대칭 이동의 결과와 완전히 포개어지도록 만들 수 있다.", "X", "선대칭 이동은 도형의 좌우 방향성을 반전시키기 때문에 2차원 평면 내에서의 이동과 회전만으로는 포갤 수 없다.", ["선대칭 이동은 도형의 좌우 방향성(Orientation)을 반전시키기 때문에, 평면 내에서의 강체 운동(이동/회전)만으로는 절대 포갤 수 없다."], "방향성과 대칭이동", STD_01_07),
    makeOx("중심이 $(a, b)$이고 반지름이 $r$인 원을 직선 $y=x$에 대하여 대칭이동한 후 $x$축 방향으로 $a-b$만큼, $y$축 방향으로 $b-a$만큼 평행이동하면 원래의 원과 항상 완벽히 일치한다.", "O", "중심이 $(b, a)$로 이동한 후 $(b+a-b, a+b-a) = (a, b)$가 되어 제자리로 돌아오고 반지름도 같기 때문이다.", ["대칭이동된 중심 (b, a)에 주어진 평행이동량을 더하면 (a, b)가 되어 원위치로 돌아옴을 계산한다."], "평행이동과 대칭의 항등 변환", STD_01_07),
    makeOx("좌표평면에서 두 점 $A, B$를 잇는 선분 $AB$가 직선 $l$에 의해 수직이등분될 때, 점 $A$를 지나는 임의의 도형을 직선 $l$에 대하여 대칭이동시킨 도형은 반드시 점 $B$를 지난다.", "O", "점 $A$의 대칭점이 $B$이므로, $A$를 포함하는 도형을 직선 $l$에 대칭이동시키면 그 결과는 반드시 $B$를 포함한다.", ["점 A의 대칭점이 B이므로, A를 포함하는 어떤 집합을 대칭이동시키면 그 결과 집합은 반드시 B를 포함하게 됨을 논리적으로 추론한다."], "점의 대칭과 집합", STD_01_07),

    // --- [동적 생성 문제 5종] ---
    (function() {
      const a = riSafe(2, 5);
      const b = a + 6; // b-a = 6
      const isO = Math.random() < 0.5;
      const ans = isO ? "O" : "X";
      const show_dist = isO ? 10 : 10 + riSafe(1, 2);
      return makeOx(`점 $A(1, ${a})$에서 직선 $y=x$ 위의 점 $P$, $x$축 위의 점 $Q$를 차례로 거쳐 점 $B(${b}, 7)$로 이동하는 최단 경로는 $${show_dist}$이다.`, ans, `대칭점 $A'(${a}, 1)$과 $B'(${b}, -7)$의 거리 $\\sqrt{(${b}-${a})^2 + (-7-1)^2} = \\sqrt{36+64} = 10$이 ${ans==="O"?"된다":"아니다"}.`, ["점 A를 직선 y=x에 대하여 대칭이동한 점과, 점 B를 x축에 대하여 대칭이동한 점을 연결한 선분의 길이를 구한다."], "두 번 꺾이는 최단 거리", STD_01_07);
    })(),
    (function() {
      const a = riSafe(1, 4) * 2; // 짝수 2, 4, 6, 8
      const R = 100; // 큰 원
      const isO = Math.random() < 0.5;
      const ans = isO ? "O" : "X";
      const S_real = (a/2) * (a/2);
      const show_S = isO ? S_real : S_real + riSafe(1, 3);
      return makeOx(`원 $x^2+y^2=${R}$의 내부에 있는 점 $A(${a}, 0)$을 지나는 무수히 많은 현들의 중점이 그리는 자취가 이루는 도형의 넓이는 $${show_S}\\pi$이다.`, ans, `현의 중점과 중심을 이은 선은 수직이므로, 원점과 $A(${a},0)$을 지름 양끝으로 하는 반지름 $${a/2}$인 원이 되어 넓이는 $${S_real}\\pi$이다.`, ["원의 중심에서 현에 내린 수선은 현을 이등분하므로, 원점과 점 A를 지름의 양 끝으로 하는 원이 자취가 됨을 이용한다."], "현의 중점의 자취 넓이", STD_01_05);
    })(),
    (function() {
      const d_base = riSafe(5, 9); // 중심거리 배수
      const r = riSafe(2, 4); // 반지름
      const isO = Math.random() < 0.5;
      const ans = isO ? "O" : "X";
      const count_real = 4 * r;
      const show_count = isO ? count_real : count_real + riSafe(2, 4);
      return makeOx(`원 $x^2+y^2=${r*r}$ 위의 점과 직선 $3x+4y-${d_base*5}=0$ 사이의 거리가 정수가 되는 원 위의 점의 개수는 총 $${show_count}$개이다.`, ans, `중심 거리가 $${d_base}$이므로 최소 거리는 $${d_base-r}$, 최대 거리는 $${d_base+r}$이다. 그 사이의 정수 거리는 각각 2개씩 존재하므로 총 $4\\times${r}=${count_real}$개이다.`, ["중심에서 직선까지의 거리 d를 구하고, 최솟값 d-r과 최댓값 d+r 사이의 정수 거리가 각각 2개씩 존재함을 파악하여 개수를 센다."], "정수 거리인 점의 개수", STD_01_05);
    })(),
    (function() {
      const sets = [[3,4,5], [6,8,10], [5,12,13]];
      const s = sets[riSafe(0, 2)];
      const a = s[0], b = s[1], hyp = s[2];
      const isO = Math.random() < 0.5;
      const ans = isO ? "O" : "X";
      const show_dist = isO ? hyp : hyp + riSafe(1, 2);
      return makeOx(`세 점 $(0, 0), (${2*a}, 0), (0, ${2*b})$을 꼭짓점으로 하는 삼각형의 외심과 수심 사이의 거리는 $${show_dist}$이다.`, ans, `수심은 직각 꼭짓점 $(0,0)$, 외심은 빗변의 중점 $(${a}, ${b})$이므로 두 점 거리는 $\\sqrt{${a}^2+${b}^2} = ${hyp}$이다.`, ["직각삼각형의 외심은 빗변의 중점이고, 수심은 직각을 낀 꼭짓점 자체가 됨을 기하학적으로 연상하여 거리를 구한다."], "외심과 수심의 거리", STD_01_01);
    })(),
    (function() {
      const p = riSafe(2, 5), q = riSafe(2, 5);
      const a = riSafe(1, 3), b = riSafe(1, 3);
      const isO = Math.random() < 0.5;
      const ans = isO ? "O" : "X";
      const rx = -q+a, ry = -p+b;
      const show_rx = isO ? rx : rx + riSafe(1, 2);
      const show_ry = isO ? ry : ry - riSafe(1, 2);
      return makeOx(`점 $(${p}, ${q})$를 원점에 대하여 대칭이동시킨 후, 다시 직선 $y=x$에 대하여 대칭이동시키고, 마지막으로 $x$축 방향으로 $${a}$, $y$축 방향으로 $${b}$만큼 평행이동시킨 점의 최종 좌표는 $(${show_rx}, ${show_ry})$이다.`, ans, `$(-${p}, -${q}) \\rightarrow (-${q}, -${p}) \\rightarrow (-${q}+${a}, -${p}+${b}) = $ $(${rx}, ${ry})$이 ${ans==="O"?"된다":"아니다"}.`, ["원점 대칭, y=x 대칭, 평행이동의 순서대로 각 변환의 규칙을 침착하게 누적 적용한다."], "복합 변환 좌표 추론", STD_01_07);
    })()
  ];

  const smartGens = {
    "01_01": function() {
      const x1 = riSafe(-3, 3), y1 = riSafe(-3, 3), dx = 3, dy = 4;
      const x2 = x1 + dx, y2 = y1 + dy;
      return { type: ["객관식", "단답형"], q: `두 점 $A(${x1}, ${y1}), B(${x2}, ${y2})$ 사이의 거리를 구하시오.`, choices: ["$3$","$4$","$5$","$6$","$7$"], ci: 2, ans: "5", sa: "5", sol: "$\\sqrt{3^2+4^2}=5$", hints: ["두 점 사이의 거리 공식을 대입하여 계산한다."], terms: "두 점 사이의 거리", std: STD_01_01 };
    }
  };

  const basicMcGens = {
    "01_01": function() { return smartGens["01_01"](); }
  };

  const lv2McGens = {
    "01_01": function() { return smartGens["01_01"](); }
  };

  const lv1EsGens = {
    "01_01": function() {
      const x1 = riSafe(-5, 0), dx = riSafe(3, 8), x2 = x1 + dx;
      const q = `수직선 위의 두 점 $A(${x1}), B(${x2})$ 사이의 거리를 구하는 과정을 서술하시오.`;
      const sol = `1) 수직선 위의 두 점 사이의 거리는 큰 좌표에서 작은 좌표를 빼서 구한다. <br>2) 따라서 $${x2} - (${x1}) = ${dx}$이다.`;
      const hts = ["수직선 위에서 두 점 사이의 거리를 구하는 정의를 이용한다.", "두 좌표의 차를 구한 뒤 절댓값을 취하여 계산한다."];
      return makeEs(q, `${dx}`, `${dx}`, sol, hts, "수직선 위의 두 점 사이의 거리", STD_01_01);
    },
    "01_02": function() {
      const m = riSafe(2, 5), b1 = riSafe(1, 3), b2 = riSafe(4, 6);
      const q = `두 직선 $y = ${m}x ${spNZ(b1)}$와 $y = ${m}x ${spNZ(b2)}$의 위치 관계를 판단하는 과정을 서술하시오.`;
      const sol = `1) 두 직선의 기울기는 모두 $${m}$으로 서로 같다. <br>2) $y$절편은 각각 $${b1}$과 $${b2}$로 서로 다르다. <br>3) 따라서 두 직선은 서로 평행하다.`;
      const hts = ["두 직선의 기울기와 y절편을 각각 비교한다.", "기울기가 같고 y절편이 다를 때의 위치 관계를 판단한다."];
      return makeEs(q, "평행", "평행", sol, hts, "두 직선의 평행 조건", STD_01_02);
    },
    "01_03": function() {
      const x = riSafe(2, 6), y = riSafe(2, 6);
      const q = `점 $(${x}, ${y})$에서 $x$축까지의 거리를 구하는 과정을 서술하시오.`;
      const sol = `1) 점 $(x, y)$에서 $x$축까지의 거리는 점의 $y$좌표의 절댓값이다. <br>2) 따라서 $|${y}| = ${y}$이다.`;
      const hts = ["점의 좌표와 축 사이의 거리 관계를 파악한다.", "축까지의 거리는 해당 좌표의 절댓값임을 이용한다."];
      return makeEs(q, `${y}`, `${y}`, sol, hts, "점과 축 사이의 거리", STD_01_03);
    },
    "01_04": function() {
      const r = riSafe(2, 5);
      const q = `중심이 원점 $(0, 0)$이고 반지름의 길이가 $${r}$인 원의 방정식을 구하는 과정을 서술하시오.`;
      const eq = `$x^2 + y^2 = ${r*r}$`;
      const sol = `1) 중심이 $(0,0)$이고 반지름이 $r$인 원의 방정식은 $x^2 + y^2 = r^2$이다. <br>2) 반지름이 $${r}$이므로 식은 ${eq}이다.`;
      const hts = ["원의 중심이 원점일 때의 원의 방정식의 표준형을 상기한다.", "반지름의 길이를 제곱하여 우변의 r^2 자리에 대입한다."];
      return makeEs(q, eq, eq, sol, hts, "원의 방정식", STD_01_04);
    },
    "01_05": function() {
      const r = riSafe(3, 5);
      const q = `원 $x^2 + y^2 = ${r*r}$와 $y$축의 교점의 좌표를 모두 구하는 과정을 서술하시오.`;
      const sol = `1) $y$축과의 교점은 $x=0$을 대입하여 구한다. <br>2) $y^2 = ${r*r}$에서 $y = \\pm ${r}$이므로 $(0, ${r}), (0, -${r})$이다.`;
      const hts = ["축과의 교점은 해당 축의 특성상 하나의 좌표가 0임을 이용한다.", "원의 방정식에 x=0을 대입하여 y값을 산출한다."];
      return makeEs(q, `(0, ${r}), (0, -${r})`, `(0,${r}),(0,-${r})`, sol, hts, "원과 축의 위치 관계", STD_01_05);
    },
    "01_06": function() {
      const x = riSafe(1, 5), y = riSafe(1, 5);
      const q = `점 $(${x}, ${y})$를 $x$축 방향으로 $-${x}$, $y$축 방향으로 $-${y}$만큼 평행이동한 점의 좌표를 구하는 과정을 서술하시오.`;
      const sol = `1) 각 좌표에 이동량을 더한다. <br>2) $(${x}-${x}, ${y}-${y}) = (0, 0)$이다.`;
      const hts = ["점의 평행이동은 좌표 성분에 이동량을 직접 더하여 구한다.", "각 좌표 성분별로 덧셈 연산을 정확히 수행한다."];
      return makeEs(q, "(0, 0)", "0,0", sol, hts, "점의 평행이동", STD_01_06);
    },
    "01_07": function() {
      const x = riSafe(2, 5), y = riSafe(2, 5);
      const q = `점 $(${x}, ${y})$를 $x$축에 대하여 대칭이동한 점의 좌표를 구하는 과정을 서술하시오.`;
      const sol = `1) $x$축 대칭이동은 $y$좌표의 부호를 바꾼다. <br>2) 따라서 $(${x}, -${y})$이다.`;
      const hts = ["축에 대한 대칭이동 시 부호가 변하는 좌표 성분을 확인한다.", "x축 대칭은 y좌표의 부호만 바뀜을 이용한다."];
      return makeEs(q, `$(${x}, -${y})$`, `${x},-${y}`, sol, hts, "점의 대칭이동", STD_01_07);
    }
  };

  const lv2EsGens = {
    "01_01": function() {
      const x1 = riSafe(-3, 3), y1 = riSafe(-3, 3), dx = riSafe(2, 6), dy = riSafe(2, 6);
      const x2 = x1 + dx, y2 = y1 + dy;
      const mx = (x1 + x2) / 2, my = (y1 + y2) / 2;
      const q = `두 점 $A(${x1}, ${y1}), B(${x2}, ${y2})$의 중점 $M$의 좌표를 구하는 과정을 서술하시오.`;
      const sol = `1) 중점의 좌표는 두 점의 좌표의 산술평균으로 구한다. <br>2) $M\\left(\\dfrac{${x1}+${x2}}{2}, \\dfrac{${y1}+${y2}}{2}\\right) = (${formatNum(mx)}, ${formatNum(my)})$이다. <br>따라서 중점의 좌표는 $(${formatNum(mx)}, ${formatNum(my)})$이다.`;
      const hints = ["선분의 중점 공식을 이용한다.", "각 좌표의 합을 2로 나누어 계산한다."];
      return makeEs(q, `(${formatNum(mx)}, ${formatNum(my)})`, `${mx},${my}`, sol, hints, "중점", STD_01_01);
    },
    "01_02": function() {
      const a = riSafe(2, 4);
      const q = `두 직선 $y = ${a}x + 1$과 $y = (k-1)x + 5$가 서로 평행할 때, 상수 $k$의 값을 구하는 과정을 서술하시오.`;
      const sol = `1) 두 직선이 평행하려면 기울기가 같고 $y$절편이 달라야 한다. <br>2) $y$절편은 각각 $1$과 $5$로 서로 다르므로 기울기만 비교한다. <br>3) $k-1 = ${a}$에서 $k = ${a+1}$이다.`;
      const hints = ["평행한 두 직선의 기울기는 서로 같음을 이용한다.", "기울기를 나타내는 계수를 비교하여 방정식을 세운다."];
      return makeEs(q, `${a+1}`, `${a+1}`, sol, hints, "평행 조건", STD_01_02);
    },
    "01_03": function() {
      const x0 = riSafe(1, 3), y0 = riSafe(1, 3);
      const q = `점 $(${x0}, ${y0})$과 직선 $4x - 3y + 5 = 0$ 사이	의 거리를 구하는 과정을 서술하시오.`;
      const dist = Math.abs(4*x0 - 3*y0 + 5) / 5;
      const sol = `1) 점과 직선 사이의 거리 공식을 이용한다. <br>2) $d = \\dfrac{|4(${x0}) - 3(${y0}) + 5|}{\\sqrt{4^2+(-3)^2}} = \\dfrac{|${4*x0-3*y0+5}|}{5} = ${formatNum(dist)}$이다.`;
      const hints = ["점직거리 공식 $d = \\dfrac{|ax_1+by_1+c|}{\\sqrt{a^2+b^2}}$을 이용한다.", "공식의 분모가 $\\sqrt{4^2+(-3)^2}=5$가 됨을 확인한다."];
      return makeEs(q, `${formatNum(dist)}`, `${dist}`, sol, hints, "점직거리", STD_01_03);
    },
    "01_04": function() {
      const a = riSafe(-2, 2), b = riSafe(-2, 2), r = riSafe(2, 4);
      const q = `중심이 $(${a}, ${b})$이고 반지름의 길이가 $${r}$인 원의 방정식을 구하는 과정을 서술하시오.`;
      const eq = `$${sqShift("x", a)} + ${sqShift("y", b)} = ${r*r}$`;
      const sol = `1) 원의 중심이 $(a, b)$이고 반지름이 $r$인 원의 방정식은 $(x-a)^2+(y-b)^2=r^2$이다. <br>2) 주어진 수치를 대입하면 ${eq}이다.`;
      const hints = ["원의 방정식의 표준형을 이용한다.", "중심의 좌표와 반지름을 식에 정확히 대입한다."];
      return makeEs(q, eq, eq, sol, hints, "원의 방정식", STD_01_04);
    },
    "01_05": function() {
      const r = riSafe(2, 4);
      const d = r + 1;
      const q = `원 $x^2 + y^2 = ${r*r}$와 직선 $y = ${d}$의 위치 관계를 판단하는 과정을 서술하시오.`;
      const sol = `1) 원의 중심 $(0,0)$에서 직선 $y=${d}$, 즉 $y${spNZ(-d)}=0$까지의 거리 $d$를 구한다. <br>2) 거리는 $${d}$이고 원의 반지름은 $${r}$이다. <br>3) 거리가 반지름보다 크므로($d > r$) 원과 직선은 만나지 않는다.`;
      const hints = ["원의 중심과 직선 사이의 거리와 반지름을 비교한다.", "판단 결과인 '만나지 않는다', '접한다', '두 점에서 만난다' 중 하나를 선택한다."];
      return makeEs(q, "만나지 않는다", "만나지 않는다", sol, hints, "위치관계", STD_01_05);
    },
    "01_06": function() {
      const x0 = riSafe(1, 4), y0 = riSafe(1, 4), dx = riSafe(1, 3), dy = riSafe(1, 3);
      const q = `점 $(${x0}, ${y0})$을 $x$축 방향으로 $${dx}$, $y$축 방향으로 $${dy}$만큼 평행이동한 점의 좌표를 구하는 과정을 서술하시오.`;
      const ans = `$(${x0+dx}, ${y0+dy})$`;
      const sol = `1) 점의 평행이동은 원래 좌표에 이동량만큼을 더하여 구한다. <br>2) $(${x0}+${dx}, ${y0}+${dy}) = (${x0+dx}, ${y0+dy})$이다.`;
      const hints = ["점의 평행이동 규칙을 적용한다.", "각 좌표 성분에 이동량을 더한다."];
      return makeEs(q, ans, ans, sol, hints, "평행이동", STD_01_06);
    },
    "01_07": function() {
      const x = riSafe(2, 5), y = riSafe(2, 5);
      const q = `점 $(${x}, ${y})$를 $x$축에 대하여 대칭이동한 점의 좌표를 구하는 과정을 서술하시오.`;
      const ans = `$(${x}, -${y})$`;
      const sol = `1) $x$축에 대하여 대칭이동하면 $x$좌표는 그대로이고 $y$좌표의 부호만 바뀐다. <br>2) 따라서 점 $(${x}, ${y})$는 $(${x}, -${y})$로 이동한다.`;
      const hints = ["$x$축 대칭이동의 규칙을 적용한다.", "$y$좌표의 부호가 어떻게 변하는지 확인한다."];
      return makeEs(q, ans, ans, sol, hints, "대칭이동", STD_01_07);
    }
  };

  const lv3McGens = {
    "01_01": function() {
      const x1 = riSafe(-2, 2), y1 = riSafe(-2, 2), x2 = x1 + 6, y2 = y1 + 9;
      const q = `두 점 $A(${x1}, ${y1}), B(${x2}, ${y2})$를 $2:1$로 내분하는 점의 좌표를 구하시오.`;
      const px = (2*x2 + 1*x1)/3, py = (2*y2 + 1*y1)/3;
      const ans = `$(${px}, ${py})$`;
      const ch = makeWrongChoices(ans, [`$(${px+1}, ${py})$`, `$(${px}, ${py-1})$`, `$(${py}, ${px})$`]);
      return makeMc(q, ch.choices, ch.ci, ans, `${px},${py}`, "내분 공식 적용", ["내분"], "내분점", STD_01_01);
    }
  };

  const lv3SaGens = {
    "01_01": function() {
      const x1 = riSafe(-5, 0), y1 = riSafe(-5, 0), m = 2, n = 1;
      const dx = 3 * riSafe(1, 3), dy = 3 * riSafe(1, 3);
      const x2 = x1 + dx, y2 = y1 + dy;
      const px = (m * x2 + n * x1) / (m + n), py = (m * y2 + n * y1) / (m + n);
      const q = `두 점 $A(${x1}, ${y1}), B(${x2}, ${y2})$를 $2:1$로 내분하는 점의 좌표를 $(a, b)$라 할 때, $a+b$의 값을 구하시오.`;
      const ans = `${px + py}`;
      const sol = `내분점 $a = \\dfrac{2(${x2}) + 1(${x1})}{3} = ${px}, b = \\dfrac{2(${y2}) + 1(${y1})}{3} = ${py}$ 이므로 합은 ${ans}입니다.`;
      return makeSa(q, ans, ans, sol, ["내분점 공식 대입", "분모 m+n 확인", "a+b 연산"], "내분점", STD_01_01);
    },
    "01_02": function() {
      const a = riSafe(2, 5);
      const q = `두 직선 $y = ${a}x + 3$과 $y = kx - 1$이 서로 수직일 때, 상수 $k$의 값을 구하시오.`;
      const ans = toFrac(-1, a);
      return makeSa(q, `$${ans}$`, `${-1/a}`, `기울기 곱은 $-1$입니다.`, ["수직 조건"], "수직", STD_01_02);
    },
    "01_03": function() {
      const x0 = riSafe(1, 4), y0 = riSafe(1, 4), d = riSafe(1, 3);
      const c = d * 5 - (3 * x0 + 4 * y0);
      const q = `점 $(${x0}, ${y0})$과 직선 $3x + 4y ${spNZ(c)} = 0$ 사이의 거리를 구하시오.`;
      return makeSa(q, `${d}`, `${d}`, `공식 대입 시 ${d}입니다.`, ["점직거리 공식"], "거리", STD_01_03);
    },
    "01_04": function() {
      const a = riSafe(-2, 2), b = riSafe(-2, 2), r = riSafe(2, 5);
      const A = -2 * a, B = -2 * b, C = a * a + b * b - r * r;
      const q = `원의 방정식 $x^2 + y^2 ${spNZ(A)}x ${spNZ(B)}y ${spNZ(C)} = 0$의 반지름의 길이를 구하시오.`;
      return makeSa(q, `${r}`, `${r}`, `표준형 변환 시 반지름은 ${r}입니다.`, ["원 표준형"], "반지름", STD_01_04);
    },
    "01_05": function() {
      const r = riSafe(2, 4);
      const q = `원 $x^2 + y^2 = ${r * r * 2}$와 직선 $y = x + k$가 접할 때, 양수 $k$의 값을 구하시오.`;
      return makeSa(q, `${2 * r}`, `${2 * r}`, `d=r 이용 시 k=${2 * r}입니다.`, ["위치관계"], "원과 직선", STD_01_05);
    },
    "01_06": function() {
      const x0 = riSafe(-2, 2), y0 = riSafe(-2, 2), a = riSafe(1, 4), b = riSafe(1, 4);
      const q = `원 $(x ${spNZ(-x0)})^2 + (y ${spNZ(-y0)})^2 = 9$를 $x$축 방향으로 $${a}$, $y$축 방향으로 $${b}$만큼 평행이동했을 때, 이동된 원의 중심의 좌표를 $(p, q)$라 하자. $p+q$의 값을 구하시오.`;
      const ans = `${x0 + a + y0 + b}`;
      return makeSa(q, ans, ans, `중심 이동 추적: $(${x0+a}, ${y0+b})$`, ["평행이동"], "이동", STD_01_06);
    },
    "01_07": function() {
      const x = riSafe(2, 6), y = riSafe(2, 6);
      const q = `점 $(${x}, ${y})$를 직선 $y=x$에 대하여 대칭이동한 후, $x$축에 대하여 대칭이동한 점의 좌표를 $(a, b)$라 할 때, $a-b$의 값을 구하시오.`;
      return makeSa(q, `${y + x}`, `${y + x}`, `$(y, -x)$이므로 $a-b=${y}-(-${x})=${y+x}$`, ["대칭이동"], "대칭", STD_01_07);
    }
  };

  const lv3EsGens = {
    "01_01": function() {
      const x1 = 0, y1 = 0, x2 = 6, y2 = 0, x3 = 3, y3 = 6;
      const q = `세 점 $A(0,0), B(6,0), C(3,6)$을 꼭짓점으로 하는 삼각형 $ABC$의 무게중심 $G$의 좌표를 구하고, 선분 $OG$($O$는 원점)를 $2:1$로 외분하는 점의 좌표를 구하는 과정을 서술하시오.`;
      const sol = `1) 무게중심 $G(3, 2)$ 도출 <br>2) 외분 공식 적용: $\\left(\\dfrac{2(3)-1(0)}{1}, \\dfrac{2(2)-1(0)}{1}\\right) = (6, 4)$`;
      const hints = ["무게중심 구하기와 외분점 공식을 단계별로 적용한다.", "먼저 무게중심 $G$의 좌표를 구한 뒤, 원점과 $G$를 끝점으로 하여 외분 공식을 대입한다.", "무게중심은 세 좌표의 평균이며, $2:1$ 외분점은 분모가 $2-1=1$이 됨을 확인한다."];
      return makeEs(q, "(6, 4)", "6,4", sol, hints, "무게중심/외분", STD_01_01);
    },
    "01_02": function() {
      const q = `두 직선 $x-y+2=0$과 $x+2y-4=0$의 교점을 지나고, 직선 $2x-y+1=0$에 수직인 직선의 방정식을 구하는 과정을 서술하시오.`;
      const sol = `1) 교점 $(0, 2)$ 도출 <br>2) 수직 기울기 $-1/2$ 도출 <br>3) 식: $y = -1/2x+2$`;
      const hints = ["직선의 교점과 수직 조건을 차례대로 이용한다.", "두 직선을 연립하여 교점을 먼저 찾아본다.", "수직인 직선의 기울기는 주어진 직선 기울기의 음의 역수($-1/m$)이다."];
      return makeEs(q, "y=-1/2x+2", "y=-1/2x+2", sol, hints, "직선의 위치관계", STD_01_02);
    },
    "01_03": function() {
      const q = `세 점 $A(1,1), B(5,1), C(3,4)$를 꼭짓점으로 하는 삼각형 $ABC$의 넓이를 점과 직선 사이의 거리 공식을 이용하여 구하는 과정을 서술하시오.`;
      const sol = `밑변 $AB=4$, 높이(점 $C$와 $y=1$ 사이 거리)$=3$. 넓이 $6$.`;
      const hints = ["삼각형의 넓이를 구하기 위해 한 변을 밑변으로, 대응하는 높이를 거리 공식으로 찾는다.", "선분 $AB$를 밑변으로 잡고, 점 $C$에서 직선 $AB$에 내린 수선의 길이를 구한다.", "거리 공식 $d = \\dfrac{|ax_1+by_1+c|}{\\sqrt{a^2+b^2}}$을 높이 계산에 적용한다."];
      return makeEs(q, "6", "6", sol, hints, "삼각형 넓이", STD_01_03);
    },
    "01_04": function() {
      const q = `중심이 $y=x$ 위에 있고 두 점 $(1,0), (3,0)$을 지나는 원의 방정식을 구하는 과정을 서술하시오.`;
      const sol = `중심 $(2,2)$, 반지름 $\\sqrt{5}$. 식: $(x-2)^2+(y-2)^2=5$`;
      const hints = ["원의 정의인 '중심에서 같은 거리에 있는 점'을 이용한다.", "중심 좌표를 $(a, a)$로 설정하고 두 점까지의 거리가 같음을 이용해 $a$를 구한다.", "점 $(1,0)$과 중심 $(a, a)$ 사이의 거리와 점 $(3,0)$과 중심 사이의 거리를 같게 둔다."];
      return makeEs(q, "(x-2)^2+(y-2)^2=5", "(x-2)^2+(y-2)^2=5", sol, hints, "원의 방정식", STD_01_04);
    },
    "01_05": function() {
      const q = `원 $x^2+y^2=10$과 직선 $x-y-2=0$이 만나서 생기는 현의 길이를 구하는 과정을 서술하시오.`;
      const sol = `거리 $d=\\sqrt{2}$, 반지름 $r=\\sqrt{10}$. 현의 절반 $\\sqrt{8}=2\\sqrt{2}$. 전체 $4\\sqrt{2}$.`;
      const hints = ["원과 직선이 만날 때 생기는 직각삼각형의 피타고라스 정리를 이용한다.", "원의 중심에서 직선까지의 거리($d$)와 원의 반지름($r$)을 먼저 구한다.", "현의 길이는 $2\\sqrt{r^2-d^2}$임을 이용한다."];
      return makeEs(q, "4\\sqrt{2}", "4sqrt(2)", sol, hints, "현의 길이", STD_01_05);
    },
    "01_06": function() {
      const q = `직선 $2x-y+3=0$을 $x$축 방향으로 $1$, $y$축 방향으로 $-2$만큼 평행이동한 직선의 방정식을 구하는 과정을 서술하시오.`;
      const sol = `$2(x-1)-(y+2)+3=0 \\Rightarrow 2x-y-1=0$`;
      const hints = ["도형의 평행이동 시 좌표 변수의 대치 규칙을 이용한다.", "식의 $x$ 대신 $(x-1)$을, $y$ 대신 $(y+2)$를 대입하여 정리한다.", "평행이동 후에도 직선의 기울기는 변하지 않음을 확인한다."];
      return makeEs(q, "2x-y-1=0", "2x-y-1=0", sol, hints, "직선의 평행이동", STD_01_06);
    },
    "01_07": function() {
      const q = `두 점 $A(1,2), B(5,4)$와 $x$축 위의 점 $P$에 대하여 $AP+PB$의 값이 최소가 될 때의 점 $P$의 좌표를 구하는 과정을 서술하시오.`;
      const sol = `$A'(1,-2)$와 $B(5,4)$를 잇는 직선의 $x$절편. $y = 1.5x-3.5 \\Rightarrow P(7/3, 0)$`;
      const hints = ["선분 합의 최솟값은 대칭이동을 통한 일직선 거리로 해결한다.", "한 점을 $x$축에 대칭시킨 후 다른 점과 연결한 직선의 방정식을 구한다.", "대칭된 점 $A'(1,-2)$와 점 $B(5,4)$를 잇는 직선의 $x$절편이 $P$이다."];
      return makeEs(q, "(7/3, 0)", "7/3,0", sol, hints, "최단거리", STD_01_07);
    }
  };

  [smartGens, basicMcGens, lv1EsGens, lv2McGens, lv2EsGens, lv3McGens, lv3SaGens, lv3EsGens].forEach(gens => {
    Object.keys(gens).forEach(k => {
      const fn = gens[k], d = fn();
      if (d) { fn._types = d.type; fn._std = d.std; fn._terms = d.terms; }
    });
  });

  _H_OX[G][U] = { 1: lv1_OX, 2: lv2_OX, 3: lv3_OX, 4: lv4_OX, 5: lv5_OX };
  _H_B[G][U] = {
    1: [...lv1_OX, ...Object.values(lv1EsGens).map(f=>f())],
    2: [...lv2_OX, smartGens["01_01"](), ...Object.values(lv2EsGens).map(f=>f())],
    3: [...lv3_OX, ...Object.values(lv3McGens).map(f=>f()), ...Object.values(lv3SaGens).map(f=>f()), ...Object.values(lv3EsGens).map(f=>f())],
    4: [...lv4_OX],
    5: [...lv5_OX]
  };

  _H_RG[G][U] = {
    1: { 
      "OX형": lv1_OX.map((_, i) => () => lv1_OX[i]), 
      "객관식": Object.values(smartGens), 
      "단답형": [], 
      "서술형": Object.values(lv1EsGens) 
    },
    2: { "OX형": lv2_OX.map((_, i) => () => lv2_OX[i]), "객관식": [()=>smartGens["01_01"]()], "단답형": [], "서술형": Object.values(lv2EsGens) },
    3: { "OX형": lv3_OX.map((_, i) => () => lv3_OX[i]), "객관식": Object.values(lv3McGens), "단답형": Object.values(lv3SaGens), "서술형": Object.values(lv3EsGens) },
    4: { "OX형": lv4_OX.map((_, i) => () => lv4_OX[i]), "객관식": [], "단답형": [], "서술형": [] },
    5: { "OX형": lv5_OX.map((_, i) => () => lv5_OX[i]), "객관식": [], "단답형": [], "서술형": [] }
  };

  if (_H_GU[G]) {
    const m2 = _H_GU[G].find(s => s.subject === "공통수학2");
    if (m2 && !m2.units.find(u => u.v === U)) m2.units.push({ v: U, d: "평면좌표·직선·원·이동" });
  }
  console.log("도형의 방정식(ULTIMATE COMPLETE) 모듈 로드 완료");
})();
